import {
  SWARM_ADMISSION_ANNOTATION_DECISION,
  SWARM_ADMISSION_ANNOTATION_PASSPORT_ID,
  SWARM_ADMISSION_ANNOTATION_PRODUCER_REVISION,
  SWARM_ADMISSION_ANNOTATION_PROOF_CELLS,
  SWARM_ADMISSION_ANNOTATION_RECOVERY_DIGEST,
  SWARM_ADMISSION_ANNOTATION_RUNTIME_ADMISSION_DESIGN_REF,
  SWARM_ADMISSION_ANNOTATION_RUNTIME_DIGEST,
  SWARM_ADMISSION_ANNOTATION_RUNTIME_KITS,
  SWARM_ADMISSION_ANNOTATION_RUNTIME_PROOF_DESIGN_REF,
  SWARM_ADMISSION_ANNOTATION_WARRANT_ID,
  SWARM_ADMISSION_ANNOTATION_WARRANT_STATUS,
  SWARM_RUNTIME_ADMISSION_DESIGN_REF,
  SWARM_RUNTIME_PROOF_DESIGN_REF,
} from '~/server/supporting-primitives-swarm-config'

export const SCHEDULE_RUNNER_NAMESPACE_ENV = 'JANGAR_POD_NAMESPACE'
export const SCHEDULE_RUNNER_SCHEDULE_NAMESPACE_ENV = 'JANGAR_SCHEDULE_NAMESPACE'
export const SCHEDULE_RUNNER_ADMISSION_CHECK_ENV = 'JANGAR_SCHEDULE_RUNNER_ADMISSION_CHECK'
export const SCHEDULE_RUNNER_RUNTIME_PROOF_CHECK_ENV = 'JANGAR_SWARM_RUNTIME_PROOF_ENFORCEMENT'
export const SCHEDULE_RUNNER_ADMISSION_STATUS_URL_ENV = 'JANGAR_SCHEDULE_RUNNER_ADMISSION_STATUS_URL'
export const SCHEDULE_RUNNER_ADMISSION_STATUS_TIMEOUT_MS_ENV = 'JANGAR_SCHEDULE_RUNNER_ADMISSION_STATUS_TIMEOUT_MS'
export const SCHEDULE_RUNNER_STAGE_CLEARANCE_ENFORCEMENT_ENV = 'JANGAR_STAGE_CLEARANCE_ENFORCEMENT'
export const SCHEDULE_RUNNER_STAGE_CLEARANCE_HOLD_STAGES_ENV = 'JANGAR_STAGE_CLEARANCE_HOLD_STAGES'
export const SCHEDULE_RUNNER_EVIDENCE_PRESSURE_MODE_ENV = 'JANGAR_EVIDENCE_PRESSURE_LEDGER_MODE'

const KUBERNETES_SERVICE_HOST_ENV = 'KUBERNETES_SERVICE_HOST'
const KUBERNETES_SERVICE_PORT_ENV = 'KUBERNETES_SERVICE_PORT'
const SWARM_STAGE_LABEL = 'swarm.proompteng.ai/stage'
const SWARM_NAME_LABEL = 'swarm.proompteng.ai/name'
export const SWARM_STAGE_CLEARANCE_ANNOTATION_PACKET_ID = 'swarm.proompteng.ai/stage-clearance-packet-id'
export const SWARM_STAGE_CLEARANCE_ANNOTATION_DECISION = 'swarm.proompteng.ai/stage-clearance-decision'
export const SWARM_STAGE_CLEARANCE_ANNOTATION_ACTION_CLASS = 'swarm.proompteng.ai/stage-clearance-action-class'
export const SWARM_STAGE_CLEARANCE_ANNOTATION_FRESH_UNTIL = 'swarm.proompteng.ai/stage-clearance-fresh-until'
export const SWARM_STAGE_CLEARANCE_ANNOTATION_REASON_CODES = 'swarm.proompteng.ai/stage-clearance-reason-codes'
export const SWARM_STAGE_CLEARANCE_ANNOTATION_MODE = 'swarm.proompteng.ai/stage-clearance-mode'
export const SWARM_STAGE_CLEARANCE_ANNOTATION_REQUIRED_REPAIR_ACTION =
  'swarm.proompteng.ai/stage-clearance-required-repair-action'
export const SWARM_STAGE_CLEARANCE_ANNOTATION_DEPENDENCY_VERDICT_ID = 'swarm.proompteng.ai/dependency-verdict-id'
export const SWARM_STAGE_CLEARANCE_ANNOTATION_DEPENDENCY_VERDICT_DECISION =
  'swarm.proompteng.ai/dependency-verdict-decision'
export const SWARM_STAGE_CLEARANCE_ANNOTATION_CLEARANCE_MARKET_LEDGER_ID =
  'swarm.proompteng.ai/clearance-market-ledger-id'
export const SWARM_STAGE_CLEARANCE_ANNOTATION_CLEARANCE_MARKET_STAGE_ADMISSION_ID =
  'swarm.proompteng.ai/clearance-market-stage-admission-id'
export const SWARM_STAGE_CLEARANCE_ANNOTATION_CLEARANCE_MARKET_STAGE_DECISION =
  'swarm.proompteng.ai/clearance-market-stage-decision'
export const SWARM_STAGE_CLEARANCE_ANNOTATION_CLEARANCE_MARKET_SELECTED_REPAIR_LOT =
  'swarm.proompteng.ai/clearance-market-selected-repair-lot'
export const SWARM_STAGE_CREDIT_ANNOTATION_LEDGER_ID = 'swarm.proompteng.ai/stage-credit-ledger-id'
export const SWARM_STAGE_CREDIT_ANNOTATION_ACCOUNT_ID = 'swarm.proompteng.ai/stage-credit-account-id'
export const SWARM_STAGE_CREDIT_ANNOTATION_DECISION = 'swarm.proompteng.ai/stage-credit-decision'
export const SWARM_STAGE_CREDIT_ANNOTATION_MODE = 'swarm.proompteng.ai/stage-credit-mode'
export const SWARM_STAGE_CREDIT_ANNOTATION_FRESH_UNTIL = 'swarm.proompteng.ai/stage-credit-fresh-until'
export const SWARM_STAGE_CREDIT_ANNOTATION_REASON_CODES = 'swarm.proompteng.ai/stage-credit-reason-codes'
export const SWARM_STAGE_CREDIT_ANNOTATION_RUNNER_SLOT_FUTURE_ID = 'swarm.proompteng.ai/runner-slot-future-id'
export const SWARM_STAGE_CREDIT_ANNOTATION_RUNNER_SLOT_FUTURE_EXPIRES_AT =
  'swarm.proompteng.ai/runner-slot-future-expires-at'
export const SWARM_STAGE_CREDIT_ANNOTATION_SELECTED_REPAIR_LOT = 'swarm.proompteng.ai/stage-credit-selected-repair-lot'
export const SWARM_EVIDENCE_PRESSURE_ANNOTATION_LEDGER_ID = 'swarm.proompteng.ai/evidence-pressure-ledger-id'
export const SWARM_EVIDENCE_PRESSURE_ANNOTATION_DECISION = 'swarm.proompteng.ai/evidence-pressure-decision'
export const SWARM_EVIDENCE_PRESSURE_ANNOTATION_MODE = 'swarm.proompteng.ai/evidence-pressure-mode'
export const SWARM_EVIDENCE_PRESSURE_ANNOTATION_FRESH_UNTIL = 'swarm.proompteng.ai/evidence-pressure-fresh-until'
export const SWARM_EVIDENCE_PRESSURE_ANNOTATION_REASON_CODES = 'swarm.proompteng.ai/evidence-pressure-reason-codes'
export const SWARM_EVIDENCE_PRESSURE_ANNOTATION_WATCH_BACKOFF_STATE =
  'swarm.proompteng.ai/evidence-pressure-watch-backoff-state'
export const SWARM_EVIDENCE_PRESSURE_ANNOTATION_REQUIRED_REPAIR_RECEIPTS =
  'swarm.proompteng.ai/evidence-pressure-required-repair-receipts'

export const buildScheduleRunnerCommand = (): string =>
  [
    'void (async () => {',
    "  const { randomUUID } = await import('node:crypto');",
    "  const { readFileSync } = await import('node:fs');",
    "  const { request } = await import('node:https');",
    `  const admissionAnnotations = ${JSON.stringify({
      passportId: SWARM_ADMISSION_ANNOTATION_PASSPORT_ID,
      decision: SWARM_ADMISSION_ANNOTATION_DECISION,
      recoveryDigest: SWARM_ADMISSION_ANNOTATION_RECOVERY_DIGEST,
      runtimeDigest: SWARM_ADMISSION_ANNOTATION_RUNTIME_DIGEST,
      runtimeKits: SWARM_ADMISSION_ANNOTATION_RUNTIME_KITS,
      producerRevision: SWARM_ADMISSION_ANNOTATION_PRODUCER_REVISION,
      warrantId: SWARM_ADMISSION_ANNOTATION_WARRANT_ID,
      warrantStatus: SWARM_ADMISSION_ANNOTATION_WARRANT_STATUS,
      proofCells: SWARM_ADMISSION_ANNOTATION_PROOF_CELLS,
      runtimeAdmissionDesignRef: SWARM_ADMISSION_ANNOTATION_RUNTIME_ADMISSION_DESIGN_REF,
      runtimeProofDesignRef: SWARM_ADMISSION_ANNOTATION_RUNTIME_PROOF_DESIGN_REF,
    })};`,
    `  const runtimeAdmissionDesignRef = ${JSON.stringify(SWARM_RUNTIME_ADMISSION_DESIGN_REF)};`,
    `  const runtimeProofDesignRef = ${JSON.stringify(SWARM_RUNTIME_PROOF_DESIGN_REF)};`,
    `  const stageClearanceAnnotations = ${JSON.stringify({
      packetId: SWARM_STAGE_CLEARANCE_ANNOTATION_PACKET_ID,
      decision: SWARM_STAGE_CLEARANCE_ANNOTATION_DECISION,
      actionClass: SWARM_STAGE_CLEARANCE_ANNOTATION_ACTION_CLASS,
      freshUntil: SWARM_STAGE_CLEARANCE_ANNOTATION_FRESH_UNTIL,
      reasonCodes: SWARM_STAGE_CLEARANCE_ANNOTATION_REASON_CODES,
      mode: SWARM_STAGE_CLEARANCE_ANNOTATION_MODE,
      requiredRepairAction: SWARM_STAGE_CLEARANCE_ANNOTATION_REQUIRED_REPAIR_ACTION,
      dependencyVerdictId: SWARM_STAGE_CLEARANCE_ANNOTATION_DEPENDENCY_VERDICT_ID,
      dependencyVerdictDecision: SWARM_STAGE_CLEARANCE_ANNOTATION_DEPENDENCY_VERDICT_DECISION,
      clearanceMarketLedgerId: SWARM_STAGE_CLEARANCE_ANNOTATION_CLEARANCE_MARKET_LEDGER_ID,
      clearanceMarketStageAdmissionId: SWARM_STAGE_CLEARANCE_ANNOTATION_CLEARANCE_MARKET_STAGE_ADMISSION_ID,
      clearanceMarketStageDecision: SWARM_STAGE_CLEARANCE_ANNOTATION_CLEARANCE_MARKET_STAGE_DECISION,
      clearanceMarketSelectedRepairLot: SWARM_STAGE_CLEARANCE_ANNOTATION_CLEARANCE_MARKET_SELECTED_REPAIR_LOT,
    })};`,
    `  const stageCreditAnnotations = ${JSON.stringify({
      ledgerId: SWARM_STAGE_CREDIT_ANNOTATION_LEDGER_ID,
      accountId: SWARM_STAGE_CREDIT_ANNOTATION_ACCOUNT_ID,
      decision: SWARM_STAGE_CREDIT_ANNOTATION_DECISION,
      mode: SWARM_STAGE_CREDIT_ANNOTATION_MODE,
      freshUntil: SWARM_STAGE_CREDIT_ANNOTATION_FRESH_UNTIL,
      reasonCodes: SWARM_STAGE_CREDIT_ANNOTATION_REASON_CODES,
      runnerSlotFutureId: SWARM_STAGE_CREDIT_ANNOTATION_RUNNER_SLOT_FUTURE_ID,
      runnerSlotFutureExpiresAt: SWARM_STAGE_CREDIT_ANNOTATION_RUNNER_SLOT_FUTURE_EXPIRES_AT,
      selectedRepairLot: SWARM_STAGE_CREDIT_ANNOTATION_SELECTED_REPAIR_LOT,
    })};`,
    `  const evidencePressureAnnotations = ${JSON.stringify({
      ledgerId: SWARM_EVIDENCE_PRESSURE_ANNOTATION_LEDGER_ID,
      decision: SWARM_EVIDENCE_PRESSURE_ANNOTATION_DECISION,
      mode: SWARM_EVIDENCE_PRESSURE_ANNOTATION_MODE,
      freshUntil: SWARM_EVIDENCE_PRESSURE_ANNOTATION_FRESH_UNTIL,
      reasonCodes: SWARM_EVIDENCE_PRESSURE_ANNOTATION_REASON_CODES,
      watchBackoffState: SWARM_EVIDENCE_PRESSURE_ANNOTATION_WATCH_BACKOFF_STATE,
      requiredRepairReceipts: SWARM_EVIDENCE_PRESSURE_ANNOTATION_REQUIRED_REPAIR_RECEIPTS,
    })};`,
    `  const swarmNameLabelName = ${JSON.stringify(SWARM_NAME_LABEL)};`,
    `  const stageLabelName = ${JSON.stringify(SWARM_STAGE_LABEL)};`,
    `  const scheduleNamespaceEnv = ${JSON.stringify(SCHEDULE_RUNNER_SCHEDULE_NAMESPACE_ENV)};`,
    `  const admissionCheckEnv = ${JSON.stringify(SCHEDULE_RUNNER_ADMISSION_CHECK_ENV)};`,
    `  const runtimeProofCheckEnv = ${JSON.stringify(SCHEDULE_RUNNER_RUNTIME_PROOF_CHECK_ENV)};`,
    `  const admissionStatusUrlEnv = ${JSON.stringify(SCHEDULE_RUNNER_ADMISSION_STATUS_URL_ENV)};`,
    `  const admissionStatusTimeoutEnv = ${JSON.stringify(SCHEDULE_RUNNER_ADMISSION_STATUS_TIMEOUT_MS_ENV)};`,
    `  const stageClearanceEnforcementEnv = ${JSON.stringify(SCHEDULE_RUNNER_STAGE_CLEARANCE_ENFORCEMENT_ENV)};`,
    `  const stageClearanceHoldStagesEnv = ${JSON.stringify(SCHEDULE_RUNNER_STAGE_CLEARANCE_HOLD_STAGES_ENV)};`,
    `  const evidencePressureModeEnv = ${JSON.stringify(SCHEDULE_RUNNER_EVIDENCE_PRESSURE_MODE_ENV)};`,
    "  const manifest = JSON.parse(readFileSync('/config/run.json', 'utf8').replaceAll('__JANGAR_DELIVERY_ID__', randomUUID()));",
    '  const terminalRunPhases = new Set(["succeeded", "failed", "error", "errored", "cancelled", "canceled", "completed"]);',
    "  const readEnv = (name) => process.env[name]?.trim() ?? '';",
    "  const asRecord = (value) => value && typeof value === 'object' && !Array.isArray(value) ? value : null;",
    '  const asArray = (value) => Array.isArray(value) ? value : [];',
    "  const asString = (value) => typeof value === 'string' && value.trim().length > 0 ? value.trim() : '';",
    '  const readNested = (value, path) => path.reduce((cursor, key) => asRecord(cursor)?.[key], value);',
    '  const parseBoolean = (value, fallback) => {',
    "    const normalized = String(value ?? '').trim().toLowerCase();",
    '    if (!normalized) return fallback;',
    "    if (['1', 'true', 'yes', 'y', 'on', 'enabled'].includes(normalized)) return true;",
    "    if (['0', 'false', 'no', 'n', 'off', 'disabled'].includes(normalized)) return false;",
    '    return fallback;',
    '  };',
    '  const parseTimeoutMs = (value) => {',
    '    const parsed = Number.parseInt(String(value ?? "").trim(), 10);',
    '    return Number.isFinite(parsed) && parsed > 0 ? parsed : 15000;',
    '  };',
    '  const summarizeError = (error) => error instanceof Error && error.message.trim().length > 0 ? error.message : String(error);',
    '  const readyStatusUrlFor = (statusUrlValue) => {',
    '    try {',
    '      const url = new URL(statusUrlValue);',
    '      url.pathname = "/ready";',
    '      url.search = "";',
    '      return url.toString();',
    '    } catch {',
    '      return "";',
    '    }',
    '  };',
    '  const normalizeRuntimeKits = (value) => {',
    '    const values = Array.isArray(value) ? value : String(value ?? "").split(",");',
    '    return values.map((entry) => String(entry).trim()).filter(Boolean).sort().join(",");',
    '  };',
    '  const writeNestedRecordValue = (value, path, key, nextValue) => {',
    '    let cursor = value;',
    '    for (const pathKey of path) {',
    '      const record = asRecord(cursor);',
    '      if (!record) return;',
    '      if (!asRecord(record[pathKey])) record[pathKey] = {};',
    '      cursor = record[pathKey];',
    '    }',
    '    const parent = asRecord(cursor);',
    '    if (parent) parent[key] = nextValue;',
    '  };',
    '  const assertFreshUntil = (value, subject) => {',
    '    const freshUntilMs = Date.parse(asString(value));',
    '    if (!Number.isFinite(freshUntilMs) || freshUntilMs <= Date.now()) {',
    '      throw new Error(`${subject} is stale`);',
    '    }',
    '  };',
    "  const consumerByStage = { discover: 'swarm_plan', plan: 'swarm_plan', implement: 'swarm_implement', verify: 'swarm_verify' };",
    "  const warrantClassByStage = { discover: 'discover', plan: 'plan', implement: 'implement', verify: 'verify' };",
    "  const stageClearanceActionClassByStage = { discover: 'dispatch_normal', plan: 'dispatch_normal', implement: 'dispatch_normal', verify: 'dispatch_normal' };",
    '  const normalizeStageClearanceMode = (value) => {',
    '    const normalized = String(value ?? "").trim().toLowerCase();',
    '    if (["disabled", "off", "false"].includes(normalized)) return "disabled";',
    '    if (["hold", "enforce", "enforced"].includes(normalized)) return "hold";',
    '    return "shadow";',
    '  };',
    '  const normalizeStageClearanceHoldStages = (value) => new Set(String(value ?? "").split(",").map((entry) => entry.trim().toLowerCase()).filter(Boolean));',
    '  const effectiveStageClearanceMode = (mode, holdStages, stage) => {',
    '    if (mode !== "hold") return mode;',
    '    if (holdStages.size === 0 || holdStages.has(stage)) return "hold";',
    '    return "shadow";',
    '  };',
    '  const isStageCreditEnforced = (mode) => mode === "hold" || mode === "enforce";',
    '  const normalizeEvidencePressureMode = (value) => {',
    '    const normalized = String(value ?? "").trim().toLowerCase();',
    '    if (["observe", "shadow", "hold", "enforce"].includes(normalized)) return normalized;',
    '    return "observe";',
    '  };',
    '  const isEvidencePressureEnforced = (mode) => mode === "hold" || mode === "enforce";',
    '  const fetchStatusPayload = async (statusUrlValue, namespace) => {',
    '    const url = new URL(statusUrlValue);',
    '    url.searchParams.set("namespace", namespace);',
    '    const timeoutMs = parseTimeoutMs(readEnv(admissionStatusTimeoutEnv));',
    '    const controller = new AbortController();',
    '    const timeout = setTimeout(() => controller.abort(), timeoutMs);',
    '    let response;',
    '    try {',
    '      response = await fetch(url, { headers: { accept: "application/json" }, signal: controller.signal });',
    '    } finally {',
    '      clearTimeout(timeout);',
    '    }',
    '    if (!response.ok) {',
    '      const body = (await response.text()).slice(0, 500);',
    '      throw new Error(`schedule status check failed: ${response.status} ${body}`);',
    '    }',
    '    const status = asRecord(await response.json());',
    '    if (!status) throw new Error("schedule status response was not an object");',
    '    return status;',
    '  };',
    '  const fetchControlPlaneStatus = async (namespace) => {',
    '    const statusUrlValue = readEnv(admissionStatusUrlEnv);',
    '    if (!statusUrlValue) {',
    '      throw new Error("schedule status check enabled but status URL is empty");',
    '    }',
    '    try {',
    '      return await fetchStatusPayload(statusUrlValue, namespace);',
    '    } catch (error) {',
    '      const readyUrlValue = readyStatusUrlFor(statusUrlValue);',
    '      if (!readyUrlValue || readyUrlValue === statusUrlValue) throw error;',
    '      console.warn(`[jangar] schedule status check failed; falling back to ready authority projection: ${summarizeError(error)}`);',
    '      return await fetchStatusPayload(readyUrlValue, namespace);',
    '    }',
    '  };',
    '  const assertCurrentAdmission = (status) => {',
    '    const annotations = asRecord(readNested(manifest, ["metadata", "annotations"])) ?? {};',
    '    const labels = asRecord(readNested(manifest, ["metadata", "labels"])) ?? {};',
    '    const swarmName = asString(labels[swarmNameLabelName]);',
    '    const stage = asString(labels[stageLabelName]).toLowerCase();',
    '    const consumer = swarmName ? consumerByStage[stage] : undefined;',
    '    const warrantExecutionClass = warrantClassByStage[stage];',
    '    if (!consumer) return;',
    '    const stampedPassportId = asString(annotations[admissionAnnotations.passportId]);',
    '    if (!stampedPassportId) throw new Error(`missing schedule admission passport annotation for ${consumer}`);',
    '    const passport = asArray(status.admission_passports).map(asRecord).find((entry) => asString(entry?.consumer_class) === consumer);',
    '    if (!passport) throw new Error(`missing current admission passport for ${consumer}`);',
    '    const currentPassportId = asString(passport.admission_passport_id);',
    '    const decision = asString(passport.decision);',
    '    if (decision !== "allow") {',
    '      throw new Error(`current schedule admission passport ${currentPassportId || stampedPassportId} is ${decision || "missing"}`);',
    '    }',
    '    assertFreshUntil(passport.fresh_until, `current schedule admission passport ${currentPassportId || stampedPassportId}`);',
    '    const currentRuntimeDigest = asString(passport.runtime_kit_set_digest);',
    '    const currentRecoveryDigest = asString(passport.recovery_case_set_digest);',
    '    const currentRuntimeKits = normalizeRuntimeKits(passport.required_runtime_kits);',
    '    const currentProducerRevision = asString(passport.producer_revision);',
    '    let currentWarrantId = "";',
    '    let currentWarrantStatus = "";',
    '    let currentRequiredProofCells = "";',
    '    let currentRuntimeProofDesignRef = "";',
    '    const runtimeKitById = new Map(asArray(status.runtime_kits).map(asRecord).filter(Boolean).map((kit) => [asString(kit.runtime_kit_id), kit]).filter(([id]) => Boolean(id)));',
    '    for (const runtimeKitId of currentRuntimeKits.split(",").filter(Boolean)) {',
    '      const runtimeKit = runtimeKitById.get(runtimeKitId);',
    '      if (!runtimeKit) throw new Error(`current schedule admission cites missing runtime kit ${runtimeKitId}`);',
    '      const runtimeDecision = asString(runtimeKit.decision);',
    '      if (runtimeDecision !== "healthy") {',
    '        throw new Error(`current schedule admission runtime kit ${runtimeKitId} is ${runtimeDecision || "missing"}`);',
    '      }',
    '      assertFreshUntil(runtimeKit.fresh_until, `current schedule admission runtime kit ${runtimeKitId}`);',
    '    }',
    '    if (parseBoolean(readEnv(runtimeProofCheckEnv), true)) {',
    '      if (!warrantExecutionClass) throw new Error(`missing recovery warrant execution class for stage ${stage || "unknown"}`);',
    '      const warrant = asArray(status.recovery_warrants).map(asRecord).find((entry) => asString(entry?.execution_class) === warrantExecutionClass && asString(entry?.admission_passport_id) === (currentPassportId || stampedPassportId) && asString(entry?.status) !== "superseded");',
    '      if (!warrant) throw new Error(`missing sealed recovery warrant for ${warrantExecutionClass} admission passport ${currentPassportId || stampedPassportId}`);',
    '      currentWarrantId = asString(warrant.recovery_warrant_id);',
    '      currentWarrantStatus = asString(warrant.status);',
    '      if (currentWarrantStatus !== "sealed") {',
    '        throw new Error(`current schedule recovery warrant ${currentWarrantId || "missing"} is ${currentWarrantStatus || "missing"}`);',
    '      }',
    '      const currentWarrantRuntimeDigest = asString(warrant.runtime_kit_digest);',
    '      if (currentWarrantRuntimeDigest !== currentRuntimeDigest) {',
    '        throw new Error(`current schedule recovery warrant ${currentWarrantId || "missing"} runtime kit digest ${currentWarrantRuntimeDigest || "missing"} does not match current passport runtime kit digest ${currentRuntimeDigest || "missing"}`);',
    '      }',
    '      const currentWarrantProducerRevision = asString(warrant.admitted_revision);',
    '      if (currentWarrantProducerRevision !== currentProducerRevision) {',
    '        throw new Error(`current schedule recovery warrant ${currentWarrantId || "missing"} admitted revision ${currentWarrantProducerRevision || "missing"} does not match current passport producer revision ${currentProducerRevision || "missing"}`);',
    '      }',
    '      const requiredProofCellIds = asArray(warrant.required_proof_cell_ids).map(asString).filter(Boolean);',
    '      if (requiredProofCellIds.length === 0) throw new Error(`current schedule recovery warrant ${currentWarrantId} does not cite required proof cells`);',
    '      const proofCellById = new Map(asArray(status.runtime_proof_cells).map(asRecord).filter(Boolean).map((cell) => [asString(cell.runtime_proof_cell_id), cell]).filter(([id]) => Boolean(id)));',
    '      for (const proofCellId of requiredProofCellIds) {',
    '        const proofCell = proofCellById.get(proofCellId);',
    '        if (!proofCell) throw new Error(`current schedule recovery warrant ${currentWarrantId} cites missing runtime proof cell ${proofCellId}`);',
    '        if (asString(proofCell.recovery_warrant_id) !== currentWarrantId) throw new Error(`runtime proof cell ${proofCellId} does not belong to recovery warrant ${currentWarrantId}`);',
    '        if (proofCell.required !== true) throw new Error(`runtime proof cell ${proofCellId} is not marked required`);',
    '        const proofStatus = asString(proofCell.status);',
    '        if (proofStatus !== "healthy") throw new Error(`runtime proof cell ${proofCellId} for warrant ${currentWarrantId} is ${proofStatus || "missing"}`);',
    '        assertFreshUntil(proofCell.expires_at, `runtime proof cell ${proofCellId}`);',
    '      }',
    '      currentRequiredProofCells = normalizeRuntimeKits(requiredProofCellIds);',
    '      currentRuntimeProofDesignRef = runtimeProofDesignRef;',
    '    }',
    '    writeNestedRecordValue(manifest, ["metadata", "annotations"], admissionAnnotations.runtimeAdmissionDesignRef, runtimeAdmissionDesignRef);',
    '    writeNestedRecordValue(manifest, ["metadata", "annotations"], admissionAnnotations.passportId, currentPassportId || stampedPassportId);',
    '    writeNestedRecordValue(manifest, ["metadata", "annotations"], admissionAnnotations.decision, decision);',
    '    writeNestedRecordValue(manifest, ["metadata", "annotations"], admissionAnnotations.runtimeDigest, currentRuntimeDigest);',
    '    writeNestedRecordValue(manifest, ["metadata", "annotations"], admissionAnnotations.recoveryDigest, currentRecoveryDigest);',
    '    writeNestedRecordValue(manifest, ["metadata", "annotations"], admissionAnnotations.runtimeKits, currentRuntimeKits);',
    '    writeNestedRecordValue(manifest, ["metadata", "annotations"], admissionAnnotations.producerRevision, currentProducerRevision);',
    '    if (currentRuntimeProofDesignRef) writeNestedRecordValue(manifest, ["metadata", "annotations"], admissionAnnotations.runtimeProofDesignRef, currentRuntimeProofDesignRef);',
    '    if (currentWarrantId) writeNestedRecordValue(manifest, ["metadata", "annotations"], admissionAnnotations.warrantId, currentWarrantId);',
    '    if (currentWarrantStatus) writeNestedRecordValue(manifest, ["metadata", "annotations"], admissionAnnotations.warrantStatus, currentWarrantStatus);',
    '    if (currentRequiredProofCells) writeNestedRecordValue(manifest, ["metadata", "annotations"], admissionAnnotations.proofCells, currentRequiredProofCells);',
    '    writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmRuntimeAdmissionDesignRef", runtimeAdmissionDesignRef);',
    '    writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmAdmissionPassportId", currentPassportId || stampedPassportId);',
    '    writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmAdmissionDecision", decision);',
    '    writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmRuntimeKitSetDigest", currentRuntimeDigest);',
    '    writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmRecoveryCaseSetDigest", currentRecoveryDigest);',
    '    writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmRequiredRuntimeKits", currentRuntimeKits);',
    '    writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmAdmissionProducerRevision", currentProducerRevision);',
    '    if (currentRuntimeProofDesignRef) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmRuntimeProofDesignRef", currentRuntimeProofDesignRef);',
    '    if (currentWarrantId) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmRecoveryWarrantId", currentWarrantId);',
    '    if (currentWarrantStatus) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmRecoveryWarrantStatus", currentWarrantStatus);',
    '    if (currentRequiredProofCells) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmRequiredProofCells", currentRequiredProofCells);',
    '  };',
    '  const applyCurrentStageClearance = (status, mode, holdStages) => {',
    '    const labels = asRecord(readNested(manifest, ["metadata", "labels"])) ?? {};',
    '    const swarmName = asString(labels[swarmNameLabelName]);',
    '    const stage = asString(labels[stageLabelName]).toLowerCase();',
    '    const actionClass = swarmName ? stageClearanceActionClassByStage[stage] : undefined;',
    '    if (!actionClass) return;',
    '    const stageMode = effectiveStageClearanceMode(mode, holdStages, stage);',
    '    const packets = asArray(status.stage_clearance_packets).map(asRecord);',
    '    const packet = packets.find((entry) => asString(entry?.swarm_name) === swarmName && asString(entry?.stage) === stage && asString(entry?.action_class) === actionClass);',
    '    if (!packet) {',
    '      const message = `missing current stage clearance packet for ${swarmName}/${stage}/${actionClass}`;',
    '      if (stageMode === "shadow") {',
    '        console.warn(`[jangar] ${message}`);',
    '        return;',
    '      }',
    '      throw new Error(message);',
    '    }',
    '    const packetId = asString(packet.packet_id);',
    '    if (!packetId) throw new Error(`current stage clearance packet for ${swarmName}/${stage}/${actionClass} is missing packet_id`);',
    '    const decision = asString(packet.decision);',
    '    const freshUntil = asString(packet.fresh_until);',
    '    const reasonCodes = normalizeRuntimeKits(asArray(packet.reason_codes).map(asString).filter(Boolean));',
    '    const requiredRepairAction = asString(packet.required_repair_action);',
    '    const dependencyVerdictId = asString(packet.dependency_verdict_ref);',
    '    const dependencyVerdictDecision = asString(packet.dependency_verdict_decision);',
    '    const clearanceMarket = asRecord(status.clearance_market_ledger);',
    '    const clearanceMarketLedgerId = asString(clearanceMarket?.ledger_id);',
    '    const stageAdmissions = asArray(clearanceMarket?.stage_admission).map(asRecord).filter(Boolean);',
    '    const stageAdmission = stageAdmissions.find((entry) => asString(entry?.stage) === stage && asString(entry?.action_class) === actionClass && (!asString(entry?.packet_ref) || asString(entry?.packet_ref) === packetId)) ?? stageAdmissions.find((entry) => asString(entry?.stage) === stage && asString(entry?.action_class) === actionClass);',
    '    const clearanceMarketStageAdmissionId = asString(stageAdmission?.admission_id);',
    '    const clearanceMarketStageDecision = asString(stageAdmission?.decision);',
    '    const clearanceMarketSelectedRepairLot = asString(stageAdmission?.selected_repair_lot_ref);',
    '    const stageCreditLedger = asRecord(status.stage_credit_ledger);',
    '    const stageCreditLedgerId = asString(stageCreditLedger?.ledger_id);',
    '    const stageCreditMode = asString(stageCreditLedger?.evidence_mode) || "observe";',
    '    const stageCreditFreshUntil = asString(stageCreditLedger?.fresh_until);',
    '    const stageCreditLedgerFreshUntilMs = Date.parse(stageCreditFreshUntil);',
    '    const stageCreditLedgerCurrent = Boolean(stageCreditLedgerId && Number.isFinite(stageCreditLedgerFreshUntilMs) && stageCreditLedgerFreshUntilMs > Date.now());',
    '    const stageCreditAccounts = asArray(stageCreditLedger?.stage_accounts).map(asRecord).filter(Boolean);',
    '    const stageCreditAccount = stageCreditAccounts.find((entry) => asString(entry?.stage) === stage && asString(entry?.action_class) === actionClass);',
    '    const stageCreditAccountId = asString(stageCreditAccount?.account_id);',
    '    const stageCreditDecision = asString(stageCreditAccount?.decision);',
    '    const stageCreditReasonCodes = normalizeRuntimeKits(asArray(stageCreditAccount?.reason_codes).map(asString).filter(Boolean));',
    '    const stageCreditSelectedRepairLot = asString(stageCreditAccount?.selected_repair_lot_ref);',
    '    const runnerSlotFutures = asArray(stageCreditLedger?.runner_slot_futures).map(asRecord).filter(Boolean);',
    '    const runnerSlotFuture = runnerSlotFutures.find((entry) => asString(entry?.account_id) === stageCreditAccountId && asString(entry?.settlement_state) === "open");',
    '    const runnerSlotFutureId = asString(runnerSlotFuture?.future_id);',
    '    const runnerSlotFutureExpiresAt = asString(runnerSlotFuture?.expires_at);',
    '    const runnerSlotFutureExpiresAtMs = Date.parse(runnerSlotFutureExpiresAt);',
    '    const runnerSlotFutureCurrent = Boolean(stageCreditLedgerCurrent && runnerSlotFutureId && Number.isFinite(runnerSlotFutureExpiresAtMs) && runnerSlotFutureExpiresAtMs > Date.now());',
    '    const stageCreditEnforced = isStageCreditEnforced(stageCreditMode);',
    '    const configuredEvidencePressureMode = normalizeEvidencePressureMode(readEnv(evidencePressureModeEnv));',
    '    const evidencePressureLedger = asRecord(status.evidence_pressure_ledger);',
    '    const evidencePressureLedgerId = asString(evidencePressureLedger?.ledger_id);',
    '    const evidencePressureMode = normalizeEvidencePressureMode(asString(evidencePressureLedger?.evidence_mode) || configuredEvidencePressureMode);',
    '    const evidencePressureFreshUntil = asString(evidencePressureLedger?.fresh_until);',
    '    const evidencePressureFreshUntilMs = Date.parse(evidencePressureFreshUntil);',
    '    const evidencePressureLedgerCurrent = Boolean(evidencePressureLedgerId && Number.isFinite(evidencePressureFreshUntilMs) && evidencePressureFreshUntilMs > Date.now());',
    '    const evidencePressureBudget = asArray(evidencePressureLedger?.action_pressure_budget).map(asRecord).filter(Boolean).find((entry) => asString(entry?.action_class) === actionClass);',
    '    const evidencePressureDecision = asString(evidencePressureBudget?.decision);',
    '    const evidencePressureReasonCodes = normalizeRuntimeKits(asArray(evidencePressureBudget?.reason_codes).map(asString).filter(Boolean));',
    '    const evidencePressureRequiredRepairReceipts = normalizeRuntimeKits(asArray(evidencePressureBudget?.required_repair_receipts).map(asString).filter(Boolean));',
    '    const evidencePressureWatchBackoff = asRecord(evidencePressureLedger?.watch_backoff_policy);',
    '    const evidencePressureWatchBackoffState = asString(evidencePressureWatchBackoff?.state);',
    '    const evidencePressureEnforced = isEvidencePressureEnforced(evidencePressureMode) || isEvidencePressureEnforced(configuredEvidencePressureMode);',
    '    if (stageCreditLedgerId) {',
    '      if (!stageCreditLedgerCurrent) {',
    '        const message = `current stage credit ledger ${stageCreditLedgerId} is stale`;',
    '        if (stageCreditEnforced) throw new Error(message);',
    '        console.warn(`[jangar] ${message}`);',
    '      }',
    '      if (!stageCreditAccountId) {',
    '        const message = `missing current stage credit account for ${swarmName}/${stage}/${actionClass}`;',
    '        if (stageCreditEnforced) throw new Error(message);',
    '        console.warn(`[jangar] ${message}`);',
    '      } else if (stageCreditDecision !== "allow" && stageCreditDecision !== "repair_only") {',
    '        const suffix = stageCreditReasonCodes ? `: ${stageCreditReasonCodes}` : "";',
    '        const message = `current stage credit account ${stageCreditAccountId} is ${stageCreditDecision || "missing"}${suffix}`;',
    '        if (stageCreditEnforced) throw new Error(message);',
    '        console.warn(`[jangar] ${message}`);',
    '      }',
    '      if ((stageCreditDecision === "allow" || stageCreditDecision === "repair_only") && !runnerSlotFutureId) {',
    '        const message = `missing open runner slot future for stage credit account ${stageCreditAccountId}`;',
    '        if (stageCreditEnforced) throw new Error(message);',
    '        console.warn(`[jangar] ${message}`);',
    '      }',
    '      if (runnerSlotFutureId) {',
    '        if (!runnerSlotFutureCurrent) {',
    '          const message = `runner slot future ${runnerSlotFutureId} is stale`;',
    '          if (stageCreditEnforced) throw new Error(message);',
    '          console.warn(`[jangar] ${message}`);',
    '        }',
    '      }',
    '    }',
    '    if (!evidencePressureLedgerId) {',
    '      const message = `missing current evidence pressure ledger for ${swarmName}/${stage}/${actionClass}`;',
    '      if (evidencePressureEnforced) throw new Error(message);',
    '      if (isEvidencePressureEnforced(configuredEvidencePressureMode)) console.warn(`[jangar] ${message}`);',
    '    } else {',
    '      if (!evidencePressureLedgerCurrent) {',
    '        const message = `current evidence pressure ledger ${evidencePressureLedgerId} is stale`;',
    '        if (evidencePressureEnforced) throw new Error(message);',
    '        console.warn(`[jangar] ${message}`);',
    '      }',
    '      if (!evidencePressureBudget) {',
    '        const message = `missing current evidence pressure budget for ${swarmName}/${stage}/${actionClass}`;',
    '        if (evidencePressureEnforced) throw new Error(message);',
    '        console.warn(`[jangar] ${message}`);',
    '      } else if (evidencePressureDecision !== "allow") {',
    '        const suffix = evidencePressureReasonCodes ? `: ${evidencePressureReasonCodes}` : "";',
    '        const message = `current evidence pressure budget ${evidencePressureLedgerId}/${actionClass} is ${evidencePressureDecision || "missing"}${suffix}`;',
    '        if (evidencePressureEnforced) throw new Error(message);',
    '        console.warn(`[jangar] ${message}`);',
    '      }',
    '    }',
    '    const freshUntilMs = Date.parse(freshUntil);',
    '    if (!Number.isFinite(freshUntilMs) || freshUntilMs <= Date.now()) {',
    '      const message = `current stage clearance packet ${packetId} is stale`;',
    '      if (stageMode !== "shadow") throw new Error(message);',
    '      console.warn(`[jangar] ${message}`);',
    '    }',
    '    if (stageMode === "hold" && decision !== "allow") {',
    '      throw new Error(`current stage clearance packet ${packetId} is ${decision || "missing"}`);',
    '    }',
    '    if (stageMode === "hold" && clearanceMarketStageDecision && clearanceMarketStageDecision !== "allow") {',
    '      const subject = clearanceMarketStageAdmissionId || clearanceMarketLedgerId || "missing";',
    '      const lot = clearanceMarketSelectedRepairLot ? `; selected repair lot: ${clearanceMarketSelectedRepairLot}` : "";',
    '      throw new Error(`current clearance market stage admission ${subject} is ${clearanceMarketStageDecision}${lot}`);',
    '    }',
    '    writeNestedRecordValue(manifest, ["metadata", "annotations"], stageClearanceAnnotations.packetId, packetId);',
    '    writeNestedRecordValue(manifest, ["metadata", "annotations"], stageClearanceAnnotations.decision, decision);',
    '    writeNestedRecordValue(manifest, ["metadata", "annotations"], stageClearanceAnnotations.actionClass, actionClass);',
    '    writeNestedRecordValue(manifest, ["metadata", "annotations"], stageClearanceAnnotations.freshUntil, freshUntil);',
    '    writeNestedRecordValue(manifest, ["metadata", "annotations"], stageClearanceAnnotations.reasonCodes, reasonCodes);',
    '    writeNestedRecordValue(manifest, ["metadata", "annotations"], stageClearanceAnnotations.mode, stageMode);',
    '    if (requiredRepairAction) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageClearanceAnnotations.requiredRepairAction, requiredRepairAction);',
    '    if (dependencyVerdictId) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageClearanceAnnotations.dependencyVerdictId, dependencyVerdictId);',
    '    if (dependencyVerdictDecision) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageClearanceAnnotations.dependencyVerdictDecision, dependencyVerdictDecision);',
    '    if (clearanceMarketLedgerId) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageClearanceAnnotations.clearanceMarketLedgerId, clearanceMarketLedgerId);',
    '    if (clearanceMarketStageAdmissionId) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageClearanceAnnotations.clearanceMarketStageAdmissionId, clearanceMarketStageAdmissionId);',
    '    if (clearanceMarketStageDecision) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageClearanceAnnotations.clearanceMarketStageDecision, clearanceMarketStageDecision);',
    '    if (clearanceMarketSelectedRepairLot) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageClearanceAnnotations.clearanceMarketSelectedRepairLot, clearanceMarketSelectedRepairLot);',
    '    if (stageCreditLedgerId) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageCreditAnnotations.ledgerId, stageCreditLedgerId);',
    '    if (stageCreditAccountId) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageCreditAnnotations.accountId, stageCreditAccountId);',
    '    if (stageCreditDecision) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageCreditAnnotations.decision, stageCreditDecision);',
    '    if (stageCreditMode) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageCreditAnnotations.mode, stageCreditMode);',
    '    if (stageCreditFreshUntil) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageCreditAnnotations.freshUntil, stageCreditFreshUntil);',
    '    if (stageCreditReasonCodes) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageCreditAnnotations.reasonCodes, stageCreditReasonCodes);',
    '    if (runnerSlotFutureCurrent) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageCreditAnnotations.runnerSlotFutureId, runnerSlotFutureId);',
    '    if (runnerSlotFutureCurrent) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageCreditAnnotations.runnerSlotFutureExpiresAt, runnerSlotFutureExpiresAt);',
    '    if (stageCreditSelectedRepairLot) writeNestedRecordValue(manifest, ["metadata", "annotations"], stageCreditAnnotations.selectedRepairLot, stageCreditSelectedRepairLot);',
    '    if (evidencePressureLedgerId) writeNestedRecordValue(manifest, ["metadata", "annotations"], evidencePressureAnnotations.ledgerId, evidencePressureLedgerId);',
    '    if (evidencePressureDecision) writeNestedRecordValue(manifest, ["metadata", "annotations"], evidencePressureAnnotations.decision, evidencePressureDecision);',
    '    if (evidencePressureMode) writeNestedRecordValue(manifest, ["metadata", "annotations"], evidencePressureAnnotations.mode, evidencePressureMode);',
    '    if (evidencePressureFreshUntil) writeNestedRecordValue(manifest, ["metadata", "annotations"], evidencePressureAnnotations.freshUntil, evidencePressureFreshUntil);',
    '    if (evidencePressureReasonCodes) writeNestedRecordValue(manifest, ["metadata", "annotations"], evidencePressureAnnotations.reasonCodes, evidencePressureReasonCodes);',
    '    if (evidencePressureWatchBackoffState) writeNestedRecordValue(manifest, ["metadata", "annotations"], evidencePressureAnnotations.watchBackoffState, evidencePressureWatchBackoffState);',
    '    if (evidencePressureRequiredRepairReceipts) writeNestedRecordValue(manifest, ["metadata", "annotations"], evidencePressureAnnotations.requiredRepairReceipts, evidencePressureRequiredRepairReceipts);',
    '    writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmStageClearancePacketId", packetId);',
    '    writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmStageClearanceDecision", decision);',
    '    writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmStageClearanceActionClass", actionClass);',
    '    writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmStageClearanceFreshUntil", freshUntil);',
    '    writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmStageClearanceReasonCodes", reasonCodes);',
    '    writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmStageClearanceEnforcement", stageMode);',
    '    if (requiredRepairAction) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmStageClearanceRequiredRepairAction", requiredRepairAction);',
    '    if (dependencyVerdictId) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmDependencyVerdictId", dependencyVerdictId);',
    '    if (dependencyVerdictDecision) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmDependencyVerdictDecision", dependencyVerdictDecision);',
    '    if (clearanceMarketLedgerId) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmClearanceMarketLedgerId", clearanceMarketLedgerId);',
    '    if (clearanceMarketStageAdmissionId) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmClearanceMarketStageAdmissionId", clearanceMarketStageAdmissionId);',
    '    if (clearanceMarketStageDecision) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmClearanceMarketStageDecision", clearanceMarketStageDecision);',
    '    if (clearanceMarketSelectedRepairLot) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmClearanceMarketSelectedRepairLotRef", clearanceMarketSelectedRepairLot);',
    '    if (stageCreditLedgerId) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmStageCreditLedgerId", stageCreditLedgerId);',
    '    if (stageCreditAccountId) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmStageCreditAccountId", stageCreditAccountId);',
    '    if (stageCreditDecision) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmStageCreditDecision", stageCreditDecision);',
    '    if (stageCreditMode) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmStageCreditMode", stageCreditMode);',
    '    if (stageCreditFreshUntil) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmStageCreditFreshUntil", stageCreditFreshUntil);',
    '    if (stageCreditReasonCodes) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmStageCreditReasonCodes", stageCreditReasonCodes);',
    '    if (runnerSlotFutureCurrent) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmRunnerSlotFutureId", runnerSlotFutureId);',
    '    if (runnerSlotFutureCurrent) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmRunnerSlotFutureExpiresAt", runnerSlotFutureExpiresAt);',
    '    if (stageCreditSelectedRepairLot) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmCreditSelectedRepairLotRef", stageCreditSelectedRepairLot);',
    '    if (evidencePressureLedgerId) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmEvidencePressureLedgerId", evidencePressureLedgerId);',
    '    if (evidencePressureDecision) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmEvidencePressureDecision", evidencePressureDecision);',
    '    if (evidencePressureMode) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmEvidencePressureMode", evidencePressureMode);',
    '    if (evidencePressureFreshUntil) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmEvidencePressureFreshUntil", evidencePressureFreshUntil);',
    '    if (evidencePressureReasonCodes) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmEvidencePressureReasonCodes", evidencePressureReasonCodes);',
    '    if (evidencePressureWatchBackoffState) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmEvidencePressureWatchBackoffState", evidencePressureWatchBackoffState);',
    '    if (evidencePressureRequiredRepairReceipts) writeNestedRecordValue(manifest, ["spec", "parameters"], "swarmEvidencePressureRequiredRepairReceipts", evidencePressureRequiredRepairReceipts);',
    '  };',
    '  const targetByKind = {',
    "  AgentRun: { group: 'agents.proompteng.ai', version: 'v1alpha1', plural: 'agentruns' },",
    "  OrchestrationRun: { group: 'orchestration.proompteng.ai', version: 'v1alpha1', plural: 'orchestrationruns' },",
    '  };',
    '  const target = targetByKind[String(manifest.kind)];',
    "  if (!target) throw new Error(`unsupported schedule target kind: ${String(manifest.kind) || 'unknown'}`);",
    `  const namespace = String((manifest?.metadata?.namespace ?? readEnv(${JSON.stringify(SCHEDULE_RUNNER_NAMESPACE_ENV)})) || 'agents');`,
    '  const scheduleNamespace = readEnv(scheduleNamespaceEnv) || namespace;',
    '  let controlPlaneStatus = null;',
    '  if (parseBoolean(readEnv(admissionCheckEnv), true)) {',
    '    controlPlaneStatus = await fetchControlPlaneStatus(scheduleNamespace);',
    '    assertCurrentAdmission(controlPlaneStatus);',
    '  }',
    '  const stageClearanceMode = normalizeStageClearanceMode(readEnv(stageClearanceEnforcementEnv));',
    '  const stageClearanceHoldStages = normalizeStageClearanceHoldStages(readEnv(stageClearanceHoldStagesEnv));',
    '  if (stageClearanceMode !== "disabled") {',
    '    try {',
    '      if (!controlPlaneStatus) controlPlaneStatus = await fetchControlPlaneStatus(scheduleNamespace);',
    '      applyCurrentStageClearance(controlPlaneStatus, stageClearanceMode, stageClearanceHoldStages);',
    '    } catch (error) {',
    '      if (stageClearanceMode !== "shadow") throw error;',
    '      console.warn(`[jangar] stage clearance shadow evidence unavailable: ${error instanceof Error ? error.message : String(error)}`);',
    '    }',
    '  }',
    "  const token = readFileSync('/var/run/secrets/kubernetes.io/serviceaccount/token', 'utf8').trim();",
    "  const ca = readFileSync('/var/run/secrets/kubernetes.io/serviceaccount/ca.crt', 'utf8');",
    `  const serviceHost = readEnv(${JSON.stringify(KUBERNETES_SERVICE_HOST_ENV)});`,
    `  const servicePort = readEnv(${JSON.stringify(KUBERNETES_SERVICE_PORT_ENV)});`,
    "  if (!serviceHost || !servicePort) throw new Error('missing kubernetes service environment');",
    '  const apiBase = `https://${serviceHost}:${servicePort}`;',
    '  const requestKubernetesJson = async (url) => await new Promise((resolve, reject) => {',
    '    const req = request(',
    '    url,',
    '    {',
    "      method: 'GET',",
    '      ca,',
    '      headers: {',
    '        authorization: `Bearer ${token}`,',
    "        accept: 'application/json',",
    '      },',
    '    },',
    '    (res) => {',
    "      let responseBody = '';",
    "      res.setEncoding('utf8');",
    "      res.on('data', (chunk) => { responseBody += chunk; });",
    "      res.on('end', () => {",
    '        if ((res.statusCode ?? 500) < 200 || (res.statusCode ?? 500) >= 300) {',
    '          reject(new Error(`schedule runner lookup failed: ${res.statusCode ?? 500} ${responseBody}`));',
    '          return;',
    '        }',
    '        try {',
    '          resolve(responseBody ? JSON.parse(responseBody) : {});',
    '        } catch (error) {',
    '          reject(error);',
    '        }',
    '      });',
    '    },',
    '    );',
    "    req.on('error', reject);",
    '    req.end();',
    '  });',
    '  const hasActiveSameStageRun = async () => {',
    '    const labels = asRecord(readNested(manifest, ["metadata", "labels"])) ?? {};',
    '    const swarmName = asString(labels[swarmNameLabelName]);',
    '    const stage = asString(labels[stageLabelName]).toLowerCase();',
    '    if (!swarmName || !stage) return false;',
    '    const listUrl = new URL(`/apis/${target.group}/${target.version}/namespaces/${namespace}/${target.plural}`, apiBase);',
    '    listUrl.searchParams.set("labelSelector", `${swarmNameLabelName}=${swarmName},${stageLabelName}=${stage}`);',
    '    const listPayload = asRecord(await requestKubernetesJson(listUrl)) ?? {};',
    '    const activeRun = asArray(listPayload.items).map(asRecord).filter(Boolean).find((item) => {',
    '      const phase = asString(readNested(item, ["status", "phase"])).toLowerCase();',
    '      return !terminalRunPhases.has(phase);',
    '    });',
    '    if (!activeRun) return false;',
    '    const activeName = asString(readNested(activeRun, ["metadata", "name"])) || "unknown";',
    '    const activePhase = asString(readNested(activeRun, ["status", "phase"])) || "unknown";',
    '    console.log(`schedule runner skipped active same-swarm stage run ${activeName} (${activePhase}) for ${swarmName}:${stage}`);',
    '    return true;',
    '  };',
    '  if (await hasActiveSameStageRun()) return;',
    '  const url = new URL(`/apis/${target.group}/${target.version}/namespaces/${namespace}/${target.plural}`, apiBase);',
    '  const body = JSON.stringify(manifest);',
    '  await new Promise((resolve, reject) => {',
    '    const req = request(',
    '    url,',
    '    {',
    "      method: 'POST',",
    '      ca,',
    '      headers: {',
    '        authorization: `Bearer ${token}`,',
    "        accept: 'application/json',",
    "        'content-type': 'application/json',",
    "        'content-length': String(Buffer.byteLength(body)),",
    '      },',
    '    },',
    '    (res) => {',
    "      let responseBody = '';",
    "      res.setEncoding('utf8');",
    "      res.on('data', (chunk) => { responseBody += chunk; });",
    "      res.on('end', () => {",
    '        if ((res.statusCode ?? 500) >= 200 && (res.statusCode ?? 500) < 300) {',
    '          resolve(undefined);',
    '          return;',
    '        }',
    '        reject(new Error(`schedule runner create failed: ${res.statusCode ?? 500} ${responseBody}`));',
    '      });',
    '    },',
    '    );',
    "    req.on('error', reject);",
    '    req.write(body);',
    '    req.end();',
    '  });',
    '})();',
  ].join('\n')
