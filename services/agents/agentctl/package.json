{
  "name": "@proompteng/agentctl",
  "version": "0.2.2",
  "description": "CLI for managing Agents via Kubernetes or Agents gRPC",
  "keywords": [
    "agents",
    "cli",
    "grpc",
    "kube",
    "kubernetes"
  ],
  "homepage": "https://github.com/proompteng/lab/tree/main/services/agents/agentctl",
  "bugs": "https://github.com/proompteng/lab/issues",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/proompteng/lab.git",
    "directory": "services/agents/agentctl"
  },
  "bin": {
    "agentctl": "./dist/agentctl.js"
  },
  "files": [
    "dist",
    "proto",
    "README.md"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "sync:proto": "bun run scripts/sync-proto.ts",
    "build": "bun run --filter @proompteng/codex build && bun run sync:proto && bun build src/index.ts --target node --outfile dist/agentctl.js && bun run scripts/add-shebang.ts dist/agentctl.js",
    "build:bin": "bun run --filter @proompteng/codex build && bun run sync:proto && bun run scripts/build-binaries.ts",
    "build:bins": "bun run --filter @proompteng/codex build && bun run sync:proto && bun run scripts/build-binaries.ts --all",
    "build:release": "bun run --filter @proompteng/codex build && bun run sync:proto && bun run scripts/build-release.ts",
    "homebrew:generate": "bun run scripts/generate-homebrew.ts",
    "lint": "bunx oxfmt --check src scripts",
    "test": "bun test",
    "validate:metadata": "bun run scripts/validate-npm-metadata.ts",
    "validate:bin": "bun run scripts/validate-binary.ts",
    "prepack": "bun run build && bun run build:bins"
  },
  "dependencies": {
    "@effect/cli": "^0.75.1",
    "@effect/cluster": "^0.58.2",
    "@effect/platform": "^0.96.1",
    "@effect/platform-bun": "^0.89.0",
    "@effect/platform-node": "^0.106.0",
    "@effect/rpc": "^0.75.1",
    "@effect/sql": "^0.51.1",
    "@effect/workflow": "^0.18.1",
    "@grpc/grpc-js": "^1.14.3",
    "@grpc/proto-loader": "^0.8.0",
    "@kubernetes/client-node": "^1.4.0",
    "@proompteng/codex": "workspace:*",
    "effect": "^3.21.2",
    "protobufjs": "^8.0.3",
    "yaml": "^2.8.4"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/bun": "1.3.14"
  },
  "packageManager": "bun@1.3.14"
}
