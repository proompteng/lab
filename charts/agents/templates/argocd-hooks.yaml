{{- if .Values.argocdHooks.enabled -}}
{{- $namespace := .Values.namespaceOverride | default .Release.Namespace -}}
{{- $serviceAccount := .Values.argocdHooks.serviceAccountName | default (include "agents.serviceAccountName" .) -}}
{{- $hookImage := .Values.argocdHooks.image | default dict -}}
{{- $hookImageDigest := $hookImage.digest | default "" -}}
{{- $smokeName := dig "metadata" "name" "" .Values.argocdHooks.smoke.agentRun -}}
{{- $labelSelector := .Values.argocdHooks.smoke.labelSelector | default (printf "agents.proompteng.ai/agent-run=%s" $smokeName) -}}
{{- if and .Values.argocdHooks.preSync.enabled (or $smokeName .Values.argocdHooks.smoke.labelSelector) -}}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "agents.fullname" . }}-smoke-cleanup
  namespace: {{ $namespace }}
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/sync-wave: {{ .Values.argocdHooks.syncWaves.preSync | quote }}
    argocd.argoproj.io/hook-delete-policy: HookSucceeded,BeforeHookCreation
  labels:
    {{- include "agents.labels" . | nindent 4 }}
spec:
  ttlSecondsAfterFinished: {{ .Values.argocdHooks.ttlSecondsAfterFinished }}
  template:
    metadata:
      labels:
        {{- include "agents.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ $serviceAccount }}
      restartPolicy: Never
      containers:
        - name: cleanup
          image: "{{ .Values.argocdHooks.image.repository }}:{{ .Values.argocdHooks.image.tag }}{{- if $hookImageDigest }}@{{ $hookImageDigest }}{{ end }}"
          imagePullPolicy: {{ .Values.argocdHooks.image.pullPolicy }}
          command:
            - sh
            - -c
          args:
            - |
              set -euo pipefail
              {{- if $smokeName }}
              kubectl -n "{{ $namespace }}" delete agentruns.agents.proompteng.ai "{{ $smokeName }}" --ignore-not-found
              {{- end }}
              {{- if $labelSelector }}
              kubectl -n "{{ $namespace }}" delete jobs -l "{{ $labelSelector }}" --ignore-not-found
              kubectl -n "{{ $namespace }}" delete configmaps -l "{{ $labelSelector }}" --ignore-not-found
              {{- end }}
{{ end }}
{{ if and .Values.argocdHooks.postSync.enabled $smokeName (gt (len .Values.argocdHooks.smoke.agentRun) 0) -}}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "agents.fullname" . }}-smoke-run
  namespace: {{ $namespace }}
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/sync-wave: {{ .Values.argocdHooks.syncWaves.postSync | quote }}
    argocd.argoproj.io/hook-delete-policy: HookSucceeded,BeforeHookCreation
  labels:
    {{- include "agents.labels" . | nindent 4 }}
spec:
  ttlSecondsAfterFinished: {{ .Values.argocdHooks.ttlSecondsAfterFinished }}
  template:
    metadata:
      labels:
        {{- include "agents.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ $serviceAccount }}
      restartPolicy: Never
      containers:
        - name: smoke
          image: "{{ .Values.argocdHooks.image.repository }}:{{ .Values.argocdHooks.image.tag }}{{- if $hookImageDigest }}@{{ $hookImageDigest }}{{ end }}"
          imagePullPolicy: {{ .Values.argocdHooks.image.pullPolicy }}
          command:
            - sh
            - -c
          args:
            - |
              set -euo pipefail
              cat <<'EOF' | kubectl -n "{{ $namespace }}" apply -f -
{{- toYaml .Values.argocdHooks.smoke.agentRun | nindent 14 }}
              EOF
              timeout {{ .Values.argocdHooks.postSync.waitTimeout }} sh -c '
                while true; do
                  phase="$(kubectl -n "{{ $namespace }}" get "agentruns.agents.proompteng.ai/{{ $smokeName }}" -o jsonpath="{.status.phase}" 2>/dev/null || true)"
                  case "$phase" in
                    Succeeded)
                      exit 0
                      ;;
                    Failed|Cancelled)
                      kubectl -n "{{ $namespace }}" get "agentruns.agents.proompteng.ai/{{ $smokeName }}" -o yaml
                      exit 1
                      ;;
                  esac
                  sleep 5
                done
              '
              {{- if .Values.argocdHooks.postSync.cleanup }}
              kubectl -n "{{ $namespace }}" delete agentruns.agents.proompteng.ai "{{ $smokeName }}" --ignore-not-found
              {{- if $labelSelector }}
              kubectl -n "{{ $namespace }}" delete jobs -l "{{ $labelSelector }}" --ignore-not-found
              kubectl -n "{{ $namespace }}" delete configmaps -l "{{ $labelSelector }}" --ignore-not-found
              {{- end }}
              {{- end }}
{{- end }}
{{- end }}
