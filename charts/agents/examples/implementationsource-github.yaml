apiVersion: agents.proompteng.ai/v1alpha1
kind: ImplementationSource
metadata:
  name: github-issues
spec:
  provider: github
  auth:
    secretRef:
      name: github-token
      key: token
  webhook:
    enabled: true
  scope:
    repository: proompteng/lab
    labels:
      - agents
