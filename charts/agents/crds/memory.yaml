apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: memories.agents.proompteng.ai
spec:
  group: agents.proompteng.ai
  names:
    kind: Memory
    plural: memories
    singular: memory
    shortNames:
      - mem
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Ready
          type: string
          jsonPath: .status.conditions[?(@.type=="Ready")].status
        - name: Type
          type: string
          jsonPath: .spec.type
        - name: Default
          type: boolean
          jsonPath: .spec.default
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: false
          required:
            - spec
          properties:
            spec:
              type: object
              required:
                - type
                - connection
              properties:
                type:
                  type: string
                  enum: [postgres, redis, weaviate, pinecone, custom]
                connection:
                  type: object
                  required:
                    - secretRef
                  properties:
                    secretRef:
                      type: object
                      required:
                        - name
                      properties:
                        name:
                          type: string
                        key:
                          type: string
                capabilities:
                  type: array
                  items:
                    type: string
                    enum: [vector, kv, blob]
                default:
                  type: boolean
                  default: false
            status:
              type: object
              properties:
                lastCheckedAt:
                  type: string
                  format: date-time
                conditions:
                  type: array
                  items:
                    type: object
                    required:
                      - type
                      - status
                      - lastTransitionTime
                    properties:
                      type:
                        type: string
                      status:
                        type: string
                      reason:
                        type: string
                      message:
                        type: string
                      lastTransitionTime:
                        type: string
                        format: date-time
                observedGeneration:
                  type: integer
