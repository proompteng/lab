services:
  haproxy:
    image: haproxy:2.9
    container_name: k8s-api-lb-altra-haproxy
    restart: unless-stopped
    ports:
      - '6444:6444'
    volumes:
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro

