apiVersion: v1alpha1
kind: UserVolumeConfig
name: local-path-provisioner
provisioning:
  diskSelector:
    match: disk.transport == 'nvme' || disk.transport == 'sata'
  minSize: 1536GiB
  maxSize: 1536GiB
