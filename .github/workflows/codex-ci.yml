name: codex-ci

on:
  pull_request:
    paths:
      - 'packages/codex/**'
      - '.github/workflows/codex-ci.yml'
  push:
    branches:
      - main
    paths:
      - 'packages/codex/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-and-build:
    uses: ./.github/workflows/common-monorepo.yml
    with:
      bun-version: 1.3.5
      node-version: 24.11.1
      install-command: bun install --frozen-lockfile --ignore-scripts
      lint-command: bunx biome check packages/codex
      oxlint-command: bun run --cwd packages/codex lint:oxlint
      oxlint-type-command: bun run --cwd packages/codex lint:oxlint:type
      test-command: bun run --filter @proompteng/codex test
