name: froussard

on:
  push:
    branches:
      - main
    paths:
      - "apps/froussard/**"
      - ".github/workflows/froussard-ci.yml"
  pull_request:
    paths:
      - "apps/froussard/**"
      - ".github/workflows/froussard-ci.yml"

jobs:
  test:
    uses: ./.github/workflows/common-monorepo.yml
    with:
      install-command: pnpm install --frozen-lockfile
      lint-command: pnpm exec biome check apps/froussard
      test-command: pnpm --filter froussard exec vitest run --coverage && node scripts/report-coverage.mjs
    secrets: inherit
