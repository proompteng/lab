# Replay Regression Harness

This directory collects scenarios for **TBS-006**. Each test stores a captured
workflow and verifies that replaying through the pure TypeScript runtime is
deterministic across versions.

> TODO(TBS-006): Populate with recorded histories and replay assertions once the
> ingestion pipeline is implemented.
