apiVersion: apiextensions.crossplane.io/v1
kind: CompositeResourceDefinition
metadata:
  name: xbudgets.budgets.proompteng.ai
spec:
  group: budgets.proompteng.ai
  names:
    kind: XBudget
    plural: xbudgets
  claimNames:
    kind: Budget
    plural: budgets
  defaultCompositionRef:
    name: budget-kubernetes
  versions:
    - name: v1alpha1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          type: object
          required:
            - spec
          properties:
            spec:
              type: object
              properties:
                limits:
                  type: object
                  properties:
                    cpu: { type: string }
                    memory: { type: string }
                    gpu: { type: string }
                    tokens: { type: integer }
                    dollars: { type: number }
                window:
                  type: object
                  properties:
                    period: { type: string }
                metadata:
                  type: object
                  additionalProperties: { type: string }
            status:
              type: object
              properties:
                phase: { type: string }
                used:
                  type: object
                  properties:
                    cpu: { type: string }
                    memory: { type: string }
                    gpu: { type: string }
                    tokens: { type: integer }
                    dollars: { type: number }
