{
  "name": "@proompteng/schematic",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "schematic": "./dist/cli.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "bunx oxfmt --check src",
    "start": "bun run build && node dist/cli.js",
    "test": "bun test",
    "lint:oxlint": "oxlint --config ../../.oxlintrc.json .",
    "lint:oxlint:type": "oxlint --config ../../.oxlintrc.json --type-aware --tsconfig ./tsconfig.json ."
  },
  "dependencies": {
    "@clack/prompts": "^1.3.0",
    "change-case": "^5.4.4",
    "execa": "^9.6.1",
    "kleur": "^4.1.5",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
