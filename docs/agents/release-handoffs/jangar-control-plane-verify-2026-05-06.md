# Jangar Control Plane Release Verification

Timestamp: 2026-05-06T19:10:00Z
Repository: `proompteng/lab`
Base: `main`
Release branch: `codex/swarm-jangar-control-plane-verify`
Observed main: `73ded2710483ed24de814c9589acec4e95f24eab`

## Owner Update Message

Jangar rollout is healthy. I selected the only current Jangar implementation PR, #5726, refreshed its anchored Codex
progress comment, confirmed it was clean and green, and squash-merged it as
`885c987a03bfb3ed0f501a9f7224daa071b066ee`. The generated GitOps promotion PR #5735 promoted image tag `885c987a` and
merged as `73ded2710483ed24de814c9589acec4e95f24eab`.

Argo reports both `jangar` and `agents` Synced/Healthy at `73ded2710483ed24de814c9589acec4e95f24eab`. The `jangar`
deployment is 1/1 ready, `agents` is 1/1 ready, `agents-controllers` is 2/2 ready, and the post-deploy verifier passed
deployment health/digest checks plus Temporal routing sync.

Next checkpoint: no release blocker remains for the selected Jangar PRs. Continue normal production monitoring and use
the GitOps rollback path below if runtime health regresses or the new shadow verdict status confuses operators.

## PRs Touched

- #5726, `feat(jangar): add material action verdict arbiter`
  - Head: `codex/swarm-jangar-control-plane`
  - Selected because it was the only current open Jangar implementation PR after refreshing the stale
    #5387/#5364/#5376 snapshot.
  - Refreshed progress comment:
    https://github.com/proompteng/lab/pull/5726#issuecomment-4390654302.
  - Squash-merged at 2026-05-06T18:53:00Z as `885c987a03bfb3ed0f501a9f7224daa071b066ee`.

- #5735, `chore(jangar): promote image 885c987a`
  - Head: `codex/jangar-release-885c987a`
  - Generated by `jangar-release` after `jangar-build-push` completed for `885c987a`.
  - Created progress comment:
    https://github.com/proompteng/lab/pull/5735#issuecomment-4391068357.
  - Merged at 2026-05-06T19:03:42Z as `73ded2710483ed24de814c9589acec4e95f24eab`.

- #5412, `feat(torghut): add proof leases and evidence gates`, remains open and was not selected for this Jangar merge
  lane. It is Torghut-scoped and still above the Codex review threshold in prior audit context.

- #5316, `chore(release/735ddbc): automated release PR`, remains open and was not selected for this Jangar merge lane.

## Comments And Conflicts

- #5726 was `MERGEABLE/CLEAN`, open, not draft, with no review requests or review threads.
- #5726 visible checks were passing or intentionally skipped before manual squash merge:
  `lint-and-typecheck / run`, `integration`, `validate`, `check_changed_files`, PR title validation, and commit lint.
- #5726 diff size was 984 additions and 11 deletions, 995 total changed lines, below the 1000-line Codex review
  threshold.
- #5735 visible checks are all passing or intentionally skipped:
  `enable`, `Lint commit messages`, `Validate PR title`, `lint`, `lint-and-typecheck / run`, and `validate`.
- Process note: repo auto-merge handled #5735 after the Jangar deploy automerge workflow enabled it. The full visible
  check set was green before rollout success was declared.

## Merge Outcomes

- #5726 merged as `885c987a03bfb3ed0f501a9f7224daa071b066ee`.
- Mainline image and CI checks for #5726:
  - `Docker Build and Push` run 25454767151 passed.
  - `jangar-build-push` run 25454767158 passed.
  - `agents-ci` run 25454767179 passed, including validate and integration.
- `jangar-release` run 25455236026 passed and opened #5735.
- #5735 merged as `73ded2710483ed24de814c9589acec4e95f24eab`.
- Mainline deployment checks after #5735:
  - `argo-lint` run 25455296959 passed.
  - `kubeconform` run 25455297157 passed.
  - `jangar-post-deploy-verify` run 25455297046 passed.

## Deployment Evidence

GitOps status from read-only `kubectl` checks:

- `jangar`: Synced, Healthy, revision `73ded2710483ed24de814c9589acec4e95f24eab`.
- `agents`: Synced, Healthy, revision `73ded2710483ed24de814c9589acec4e95f24eab`.

Workload readiness:

- `deployment/jangar` in namespace `jangar`: 1/1 ready, 1 updated.
- `deployment/agents` in namespace `agents`: 1/1 ready, 1 updated.
- `deployment/agents-controllers` in namespace `agents`: 2/2 ready, 2 updated.

Image evidence:

- `jangar`: `registry.ide-newton.ts.net/lab/jangar:885c987a@sha256:58a03e02e5d28314d8315d1d57f046592c18168e52c0fe8874b2885ffe2e2a15`.
- `agents`: `registry.ide-newton.ts.net/lab/jangar-control-plane:885c987a@sha256:cea853106207c590301f2b47b5d9afe62003eb3aa4edfd35874f91c11c91d9b7`.
- `agents-controllers`: `registry.ide-newton.ts.net/lab/jangar:885c987a@sha256:58a03e02e5d28314d8315d1d57f046592c18168e52c0fe8874b2885ffe2e2a15`.

Local validation:

- `kubectl rollout status -n jangar deployment/jangar --timeout=5m` passed.
- `kubectl rollout status -n agents deployment/agents --timeout=180s` passed.
- `kubectl rollout status -n agents deployment/agents-controllers --timeout=180s` passed.
- `bun run packages/scripts/src/jangar/verify-deployment.ts --require-synced --expected-revision 73ded2710483ed24de814c9589acec4e95f24eab --expected-revision-mode ancestor --health-attempts 3 --health-interval-seconds 2 --digest-attempts 3 --digest-interval-seconds 2` passed and verified the expected digest, serving/swarm admission passports, runtime kit digests, and runtime image refs.

Event notes:

- `jangar` emitted a transient readiness warning while the new pod was starting. Final pod state is `2/2 Running` with
  zero restarts.
- `agents` emitted transient readiness warnings on terminating old controller pods during replacement. Final deployments
  are healthy and updated; new pods have zero restarts.

## Risks And Rollback Path

Risks:

- The new material-action verdict arbiter is a shadow/status and receipt projection. A conservative or permissive verdict
  could confuse operators if interpreted as enforcement before downstream consumers are wired.
- #5735 auto-merge relied on repository automation. The full visible check set was green before rollout success was
  declared, but branch protection should be reviewed if every visible check is intended to block auto-merge.

Rollback path:

- Revert #5735 to restore the previous GitOps image/tag/digest in `argocd/applications/jangar/kustomization.yaml`,
  `argocd/applications/jangar/deployment.yaml`, and `argocd/applications/agents/values.yaml`.
- If code rollback is needed, revert #5726 and redeploy via the same Jangar release workflow.
- Operationally, ignore `material_action_verdict_epoch` and continue relying on dependency quorum, failure-domain leases,
  action SLO budgets, and controller witnesses.
- The `jangar-post-deploy-verify` workflow can open an automatic rollback PR if a mainline Jangar promotion fails.

## Next Action

No release blocker remains for the selected Jangar PRs. Monitor normal production signals for the next control-plane
cycle and use the GitOps revert path if the new verdict status creates operator confusion or runtime health regresses.
