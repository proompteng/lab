# Bun 1.3.14 Upgrade Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade the repository from Bun 1.3.13 to the latest stable Bun release, 1.3.14, and roll it out through CI/CD.

**Architecture:** Treat Bun as a repo-wide runtime contract. Update source-of-truth package metadata, CI setup actions, Docker image tags and ARGs, bootstrap scripts, deployment manifests, and user-facing tooling docs together so local, CI, image-build, and GitOps paths converge on the same version. Limit image publish rollout to applications that are enabled by Argo CD ApplicationSet selectors; disabled entries must not drive image publish jobs.

**Tech Stack:** Bun 1.3.14, Node 24.11.1, GitHub Actions, Docker/oven-bun images, Argo CD manifests, Terraform/Coder bootstrap, Oxfmt/Oxlint, Bun tests.

---

### Task 1: Confirm Upstream Version And Baseline

**Files:**

- Modify: none

- [x] **Step 1: Confirm latest stable release**

Run: `gh api repos/oven-sh/bun/releases/latest --jq '{tag_name,name,published_at,prerelease,draft,html_url}'`

Expected: `bun-v1.3.14`, `prerelease=false`, `draft=false`.

- [x] **Step 2: Confirm local Bun can run the target**

Run: `bun --version`

Expected: `1.3.14`.

- [x] **Step 3: Create a fresh branch**

Run: `git switch -c codex/upgrade-bun-1.3.14 origin/main`

Expected: clean worktree on a branch created from current `origin/main`.

### Task 2: Update Runtime Pins

**Files:**

- Modify: `.github/workflows/*.yml`
- Modify: `.github/workflows/*.yaml`
- Modify: `package.json`
- Modify: package-level `package.json` files that pin `@types/bun`, `bun-types`, or `packageManager`
- Modify: `**/Dockerfile`
- Modify: `argocd/applications/**/*.yaml`
- Modify: `kubernetes/coder/main.tf`
- Modify: `scripts/setup-workspace.sh`
- Modify: documentation files that describe the repo Bun version
- Do not modify: `packages/temporal-bun-sdk/tests/replay/corpus/manifest.json` because those values are historical fixture metadata

- [x] **Step 1: Replace repo-owned Bun 1.3.13 pins with 1.3.14**

Run a tracked-file replacement that excludes `bun.lock` and replay corpus history.

- [x] **Step 2: Regenerate lockfile**

Run: `bun install --frozen-lockfile`

Expected: if frozen lock rejects the package metadata changes, rerun `bun install` to regenerate `bun.lock`, then verify the resulting diff only advances Bun type package entries to 1.3.14.

### Task 3: Validate Upgrade

**Files:**

- Modify: none unless validation exposes a required fix

- [x] **Step 1: Check remaining stale pins**

Run: `rg -n "1\\.3\\.13|bun-version:" --hidden --glob '!**/.git/**' --glob '!**/node_modules/**'`

Expected: no stale 1.3.13 pins except historical replay corpus metadata.

- [x] **Step 2: Format/lint changed TypeScript and docs surfaces**

Run: `bunx oxfmt --check` on changed TypeScript/Markdown/JSON files where applicable.

Expected: pass.

- [x] **Step 3: Run focused tests for changed runtime templates**

Run targeted Bun tests for Dockerfile/template assertions that changed.

Expected: pass.

- [x] **Step 4: Parse workflow YAML**

Run a YAML parser over changed `.github/workflows/*` files.

Expected: all changed workflows parse.

### Task 4: Rollout Through CI

**Files:**

- Modify: `.github/PULL_REQUEST_TEMPLATE.md` only if the template changed upstream; otherwise use it as input

- [ ] **Step 1: Commit**

Run: `git add <changed files> && git commit -m "chore: upgrade bun to 1.3.14"`

- [ ] **Step 2: Push and open PR**

Use `.github/PULL_REQUEST_TEMPLATE.md`, fill every section, scan for placeholders, then run `gh pr create`.

- [ ] **Step 3: Wait for CI**

Run: `gh pr checks <pr> --watch -R proompteng/lab`

Expected: required checks pass.

- [ ] **Step 4: Merge**

Run: `gh pr merge <pr> --squash -R proompteng/lab`

Expected: PR merged to `main`; downstream build/deploy workflows pick up Bun 1.3.14 pins.

- [x] **Step 5: Correct rollout scope from Argo CD ApplicationSets**

Source of truth:

- `argocd/applicationsets/product.yaml` enables `proompteng`, `app`, and `docs`.
- `argocd/applicationsets/product.yaml` disables `cms`, `kitty-krew`, `reviseur`, and `khoshut`.
- `argocd/applicationsets/cdk8s.yaml` disables `bonjour`.
- `alchimie` is not a standalone enabled Argo CD application in the ApplicationSets.

Expected: `.github/workflows/product-nix-images.yml` publishes only enabled ApplicationSet image targets covered by that workflow: `proompteng`, `app`, and `docs`. Manual dispatch can also regenerate only those three enabled targets after the scope fix merges.
