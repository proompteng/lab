# Torghut quant release gate - 2026-05-13

Release engineer: Julian Hart
Repository: `proompteng/lab`
Branch: `codex/swarm-torghut-quant-verify`
Base: `main`
Owner channel: `swarm://owner/trading`
Last refreshed: 2026-05-13T08:20:00Z

## Owner update message

#6361 and #6364 are merged and rolled out. #6361 shipped the Torghut zero-notional repair executor guard that requires
`jangar.repair-lot-dispatch-ticket.v1` before admitted runner-backed repairs can run. #6364 promoted the resulting
Torghut image `843c92cb` to GitOps.

Rollout gate is green. `torghut` and `torghut-options` are `Synced` / `Healthy` at
`df7def95a24ef063561f68b9cd78f753574b03ab`; `torghut-post-deploy-verify` passed; `torghut-00339`,
`torghut-sim-00437`, `torghut-options-catalog`, and `torghut-options-enricher` are ready on
`sha256:d8a0f28d22bccf28ced6032a6465e32ab715a830e4eb07425ede3d9f7d068036` with zero restarts.

Runtime remains capital-safe rather than revenue-ready. The release improves `capital_gate_safety` by making admitted
repair execution fail closed without a valid dispatch ticket, but live revenue impact is still blocked by
`simple_submit_disabled` and `alpha_readiness_not_promotion_eligible`; `accepted_routeable_candidate_count` is `0` and
`zero_notional_or_stale_evidence_rate` is `1.0`.

## Governing requirement

- `docs/agents/designs/186-jangar-repair-bid-admission-and-settlement-custody-2026-05-13.md` requires a settled
  `repair_lot_dispatch_ticket` and rejects raw-bid dispatch without a compacted lot id.
- `docs/torghut/design-system/v6/190-torghut-repair-bid-settlement-and-routeability-proof-compaction-2026-05-13.md`
  requires bounded zero-notional compacted lots with dedupe key, TTL/runtime, output receipt, and `max_notional=0`.
- Runtime objective: increase routeable post-cost profit evidence and live trading readiness without weakening capital
  safety.

## Open PR enumeration

- #6361 `fix(torghut): require repair dispatch tickets`
  - Selected as the only live open Torghut PR in the refreshed queue.
  - Clean merge state, no review threads, no requested changes, and all visible checks successful or skipped.
  - Value gates: `capital_gate_safety` and `zero_notional_or_stale_evidence_rate`.
- #6364 `chore(torghut): promote image 843c92cb`
  - Generated by `torghut-release` after #6361 merged and `torghut-build-push` succeeded.
  - Selected as the required GitOps promotion PR for the merged implementation.
- #6363 and #6357
  - Jangar control-plane lane PRs, not selected for Torghut quant release verification.
- #6214, #6215, and #6219
  - Older automated release PRs, not selected because they did not advance the Torghut quant value gates in this gate.

No Torghut PRs remained open after #6364 merged.

## PRs touched

- #6361 `fix(torghut): require repair dispatch tickets`
  - Updated the anchored `<!-- codex:progress -->` comment using
    `services/jangar/scripts/codex/codex-progress-comment.ts`.
  - Verified `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`, zero review threads, and no requested changes.
  - Squash-merged at `843c92cba96090031b1fcfa0b7eb6eed4fdf7b89`.
- #6364 `chore(torghut): promote image 843c92cb`
  - Promoted source commit `843c92cba96090031b1fcfa0b7eb6eed4fdf7b89`.
  - Promoted digest `sha256:d8a0f28d22bccf28ced6032a6465e32ab715a830e4eb07425ede3d9f7d068036`.
  - Added the existing `do-not-automerge` label after the helper automerge job raced itself and failed while GitHub
    still reported the PR status as unstable.
  - Reran the failed helper job under the opt-out label; the stale failure was replaced by a successful no-op run.
  - Verified final check rollup had only successful or skipped jobs.
  - Squash-merged at `df7def95a24ef063561f68b9cd78f753574b03ab`.

## Comments and conflicts resolved

- #6361 and #6364 had no review threads.
- #6361 and #6364 had clean merge states at merge time.
- #6364 had one workflow blocker: `torghut-deploy-automerge / enable` failed at `Enable squash auto-merge` with
  `GraphQL: Pull request Pull request is in unstable status`.
- Resolution: used the existing opt-out label to make the helper rerun as a no-op, reran the failed workflow, and merged
  only after the GraphQL rollup and `gh pr checks` showed no failed, cancelled, or pending jobs.
- No direct production workload mutation was made from the local shell. Promotion went through #6364 and Argo CD.

## Validation

- PASS: #6361 PR checks:
  - `torghut-ci / Changed-file plan`.
  - `torghut-ci / Bytecode + lint + migration guard`.
  - `torghut-ci / Pyright`.
  - `torghut-ci / Pytest shard 0`.
  - `torghut-ci / Pytest shard 1`.
  - `torghut-ci / Pytest shard 2`.
  - `torghut-ci / Pytest shard 3`.
  - `torghut-ci / Bytecode + pytest + coverage`.
  - `torghut-ci / Quality signals (complexity + security)`.
  - `CI / check_changed_files`.
  - Semantic commit and PR title checks.
- PASS: #6361 local/check evidence recorded in the PR body:
  - `uv sync --frozen --extra dev`.
  - `uv run --frozen ruff format --check app/main.py app/trading/zero_notional_repair_executor.py tests/test_zero_notional_repair_executor.py tests/test_trading_api.py`.
  - `uv run --frozen ruff check app/main.py app/trading/zero_notional_repair_executor.py tests/test_zero_notional_repair_executor.py tests/test_trading_api.py`.
  - `uv run --frozen pytest -q tests/test_zero_notional_repair_executor.py tests/test_trading_api.py -k zero_notional_repair`.
  - `uv run --frozen pytest --cov --cov-branch --cov-report=term-missing --cov-report=xml`.
  - `uv run --frozen python scripts/check_diff_coverage.py --coverage-xml coverage.xml --threshold 90`.
  - `uv run --frozen pyright --project pyrightconfig.json`.
  - `uv run --frozen pyright --project pyrightconfig.alpha.json`.
  - `uv run --frozen pyright --project pyrightconfig.scripts.json`.
  - `bunx oxfmt --check docs/agents/designs/186-jangar-repair-bid-admission-and-settlement-custody-2026-05-13.md docs/torghut/design-system/v6/190-torghut-repair-bid-settlement-and-routeability-proof-compaction-2026-05-13.md`.
- PASS: #6361 main checks after merge:
  - `torghut-build-push` run `25786227333`.
  - `torghut-ci` run `25786227321`.
  - `torghut-release` run `25786322585`.
- PASS: #6364 release PR checks:
  - `argo-lint / lint`.
  - `kubeconform / validate`.
  - `torghut-ci / Changed-file plan`.
  - `torghut-ci / Release manifest changes`.
  - Semantic commit and PR title checks.
  - `torghut-deploy-automerge / enable` after opt-out rerun.
- PASS: #6364 main checks after merge:
  - `argo-lint` run `25786724124`.
  - `kubeconform` run `25786724056`.
  - `torghut-ci` run `25786724104`.
  - `torghut-post-deploy-verify` run `25786724072`.

## Deployment evidence

- Argo CD:
  - `torghut`: `Synced`, `Healthy`, revision `df7def95a24ef063561f68b9cd78f753574b03ab`, operation `Succeeded`
    at `2026-05-13T08:12:55Z`.
  - `torghut-options`: `Synced`, `Healthy`, revision `df7def95a24ef063561f68b9cd78f753574b03ab`, operation
    `Succeeded` at `2026-05-13T08:10:10Z`.
- Image promotion:
  - Tag: `843c92cb`.
  - Digest: `sha256:d8a0f28d22bccf28ced6032a6465e32ab715a830e4eb07425ede3d9f7d068036`.
  - Source commit embedded by runtime status: `843c92cba96090031b1fcfa0b7eb6eed4fdf7b89`.
- Workload readiness:
  - `deployment/torghut-00339-deployment`: `1/1` ready on the promoted digest.
  - `deployment/torghut-sim-00437-deployment`: `1/1` ready on the promoted digest.
  - `deployment/torghut-options-catalog`: `1/1` ready on the promoted digest.
  - `deployment/torghut-options-enricher`: `1/1` ready on the promoted digest.
  - New `torghut-00339` and `torghut-sim-00437` pods: both containers ready with zero restarts.
  - `torghut-db-migrations-z56qv`: completed successfully.
- Event review:
  - Expected startup/readiness probe warnings occurred during rollout warm-up.
  - Final revision events show `RevisionReady` for `torghut-00339` and `torghut-sim-00437`.
  - Old revisions scaled down after new revisions became ready.
  - No final CrashLoopBackOff, ImagePullBackOff, or restart churn was observed on promoted pods.
- Runtime endpoint checks from `torghut-post-deploy-verify`:
  - `deployment/torghut-ws` rollout succeeded.
  - `torghut.torghut.svc.cluster.local/trading/status` returned HTTP 200.
  - `torghut.torghut.svc.cluster.local/trading/revenue-repair` returned evidence and was uploaded as artifact
    `torghut-revenue-repair-25786724072-1`.
  - `/readyz` returned HTTP 503 but was accepted as `repair_only_zero_notional`; this is the expected capital-safety
    state, not a rollout failure.

## Runtime and value-gate evidence

- Revenue readiness:
  - `revenue_ready=false`.
  - `business_state=repair_only`.
  - `health.readyz_status=degraded`.
  - `health.readyz_ok=false`.
- Capital gate:
  - `live_submission_allowed=false`.
  - `live_submission_reason=simple_submit_disabled`.
  - `capital_stage=shadow`.
  - `capital_state=zero_notional`.
  - `max_notional=0`.
  - `simple_lane_orders_submitted_total=0`.
- Routeability:
  - `accepted_routeable_candidate_count=0`.
  - `routeable_candidate_count=0` in the repair-bid settlement ledger.
  - `zero_notional_or_stale_evidence_rate=1.0`.
  - `selected_repair_bid_count=39`.
  - `compacted_lot_count=6`.
  - `selected_lot_count=5`.
  - `dispatchable_lot_count=3`.
- Execution quality:
  - `fill_tca_or_slippage_quality` remains on hold for stale active-session execution samples and missing non-promoting
    expected-shortfall samples.
  - TCA evidence: `order_count=7334`, `filled_execution_count=7245`, `avg_abs_slippage_bps=13.820363759302968`.
  - Route universe summary: one probing symbol, four blocked symbols, and three missing symbols.
- Source freshness:
  - Source freshness book state is `current`.
  - Options catalog freshness summary was unavailable during runtime status due a query timeout, but the Options catalog
    and enricher workloads are ready on the promoted digest.
- Residual blockers:
  - `alpha_readiness_not_promotion_eligible`.
  - `simple_submit_disabled`.

## Risks

- Runtime is not revenue-live. The smallest blocker preventing revenue impact is the live submission gate:
  `simple_submit_disabled` plus `alpha_readiness_not_promotion_eligible`; accepted routeable candidates remain `0`.
- `/readyz` is degraded by design while Torghut is in `repair_only_zero_notional`; this preserves capital safety but keeps
  revenue impact blocked.
- The revenue-repair rollout image book still reports `image_digest_missing` and `route_adjacent_workload_proof_missing`
  even though cluster workload images prove the promoted digest. That is a runtime evidence gap to repair, not a failed
  GitOps rollout.
- Options catalog freshness summary timed out inside runtime status. The workloads are healthy, but the query timeout
  remains an operator visibility risk.
- GitHub Actions emitted Node.js 20 deprecation annotations. They did not block the gate.

## Rollback path

- GitOps rollback: revert #6364 or open a PR restoring the previous Torghut digest
  `sha256:a2c403ee2afa5f8e789b8b73d81876755e85aac5c897305edff1f7a60ef548db`, then let Argo CD reconcile.
- Source rollback: revert #6361 through a PR, allow `torghut-build-push` and `torghut-release` to publish a revert image,
  and verify `torghut-post-deploy-verify`.
- Runtime safety rollback: keep `max_notional=0`, keep `TRADING_AUTONOMY_ENABLED=false`, and keep live submissions
  disabled until routeability and alpha-readiness repairs pass.

## Next action

Use the three dispatchable zero-notional compacted repair lots to retire evidence blockers without widening capital.
The next revenue-impact gate is increasing `accepted_routeable_candidate_count` above `0` while preserving
`capital_gate_safety`; the smallest current blocker is `simple_submit_disabled` with
`alpha_readiness_not_promotion_eligible`.
