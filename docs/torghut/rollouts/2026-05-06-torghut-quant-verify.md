# Torghut quant verifier release gate - 2026-05-06

Release engineer: Julian Hart
Repository: `proompteng/lab`
Branch: `codex/swarm-torghut-quant-verify`
Base: `main`
Owner channel: `swarm://owner/trading`
Last refreshed: 2026-05-06T21:27:31Z

## Owner update message

Torghut rollout state is go for the current production image promoted by #5752 and no-go for #5412.

#5747, `chore(torghut): promote image 8ab388f8`, was conflict-free, comment-clean, and green before
merge. It squash-merged at `99462489a72abdca059d62cb7a7a31aae597d400` and promoted digest
`sha256:fb2b73e30a441c1478d3bf12c8087cfbad28c2425aa4b635d88c72a544e91acd`. Mainline
`torghut-ci` passed after a stale concurrency cancellation was rerun, but
`torghut-post-deploy-verify` did not reach the expected Synced/Healthy revision before timeout. The
rollback workflow opened #5753.

#5752, `chore(torghut): promote image c2a696c5`, superseded #5747 and is the current healthy release.
It squash-merged at `a2c74e20875a3faca77c6651d999a0dfd3465183` and promoted digest
`sha256:fcc71697d605875c037aec7a12f35f73dd489f0aaebb6087a51a6e4ec89881be`. Mainline
`torghut-ci`, `torghut-post-deploy-verify`, `argo-lint`, and `kubeconform` passed on that commit.
Argo now reports `torghut` and `torghut-options` Synced/Healthy at current main revision
`87e7e222b5dfa4879f27d0e0a0e2975a7237bc5b`, which includes #5752. The active live, sim, options
catalog, and options enricher workloads are ready on the promoted digest.

#5753, the rollback PR generated from #5747's failed postdeploy, was closed stale after #5752 passed
postdeploy and became the healthy production state. It must not be merged unless a new rollback is
created against a currently failed head.

#5412 remains held. It is conflict-free and green on head
`d2f69c4e239a51e63b016c517ef45b56f059ef76`, but the refreshed diff is 4,123 changed lines and the
required large-diff Codex review has not posted. The most recent connector response to the review
request was the Codex code-review usage-limit blocker. The smallest unblocker is restored Codex
review capacity or an explicit maintainer waiver of the large-diff review gate.

## Open PR enumeration

- #5412, `feat(torghut): add proof leases and evidence gates`, remains the open Torghut quant runtime
  PR. It is selected for release review and held on the large-diff review gate.
- #5742, `docs(torghut): refresh quant verify gate`, is this audit PR. It records the release
  captain evidence and has no runtime rollout impact.
- #5747, `chore(torghut): promote image 8ab388f8`, was selected and merged, but its postdeploy
  verification failed before #5752 superseded it.
- #5752, `chore(torghut): promote image c2a696c5`, was selected as the unblock-first superseding
  promotion and is merged with a healthy rollout.
- #5753, `chore(torghut): rollback failed promotion`, was generated by the failed #5747 postdeploy
  workflow and was closed stale after #5752 passed.
- #5316, `chore(release/735ddbc): automated release PR`, remained open but was not selected because
  it only bumps the docs image tag, not Torghut.

## Gate decisions

#5747 was go for merge and no-go for final rollout:

- PR checks passed before merge: argo-lint, kubeconform, Torghut Pyright, Torghut quality signals,
  Torghut bytecode + pytest + coverage, Semantic PR title, and Semantic commit messages.
- Squash merge commit: `99462489a72abdca059d62cb7a7a31aae597d400`.
- Mainline `torghut-ci` run `25460680115` passed after cancelling stale run `25460444104` and
  rerunning the current head.
- Mainline `torghut-post-deploy-verify` run `25460680124` failed because Argo application `torghut`
  did not become Synced/Healthy with expected revision `99462489a72abdca059d62cb7a7a31aae597d400`
  before timeout.
- Rollback PR #5753 opened automatically from that failure.

#5752 was go for merge and go for rollout:

- PR checks passed before merge: argo-lint, kubeconform, Torghut Pyright, Torghut quality signals,
  Torghut bytecode + pytest + coverage, Semantic PR title, and Semantic commit messages.
- Squash merge commit: `a2c74e20875a3faca77c6651d999a0dfd3465183`.
- Mainline `torghut-ci` run `25461512947` passed.
- Mainline `torghut-post-deploy-verify` run `25461512957` passed.
- Mainline `argo-lint` run `25461512958` passed.
- Mainline `kubeconform` run `25461512942` passed.
- Argo and Kubernetes readiness checks confirm the active Torghut workloads are on digest
  `sha256:fcc71697d605875c037aec7a12f35f73dd489f0aaebb6087a51a6e4ec89881be`.

#5753 was no-go for merge:

- The rollback was generated for the older #5747 failure.
- #5752 then advanced production and passed postdeploy on the newer digest.
- Merging #5753 after #5752 would have rolled back a healthy current release to stale state, so the
  PR was closed with a stale-rollback explanation.

#5412 remains no-go for merge:

- GitHub reported `mergeable=MERGEABLE` and `mergeStateStatus=CLEAN` during this release pass.
- Current head: `d2f69c4e239a51e63b016c517ef45b56f059ef76`.
- Size: 4,097 additions and 26 deletions across 22 files.
- Visible checks were pass or intentionally skipped: Torghut Pyright, Torghut quality signals,
  Torghut bytecode + pytest + coverage, argo-lint, kubeconform, changed-files, Semantic PR title,
  and Semantic commit messages.
- Reviews: zero posted reviews and zero review threads.
- Latest Codex review request:
  https://github.com/proompteng/lab/pull/5412#issuecomment-4391597252.
- Latest connector blocker:
  https://github.com/proompteng/lab/pull/5412#issuecomment-4391598936.

## PRs touched

- #5747: selected as a generated GitOps promotion; verified green, squash-merged, reran cancelled
  mainline CI, and captured failed postdeploy evidence.
- #5752: selected as the superseding GitOps promotion; verified green, squash-merged, and verified
  healthy rollout through mainline CI, postdeploy, Argo, workloads, pods, and events.
- #5753: inspected as an automatically generated rollback, then closed stale because #5752 had
  already passed rollout verification.
- #5412: rechecked as the remaining runtime PR and kept blocked on the large-diff Codex review gate.
- #5742: updated as the durable audit PR for this release pass.
- #5316: inspected and explicitly not selected because it is a docs release PR.

## Comments and conflicts

- #5747 and #5752 had no unresolved review threads or blocking comments before merge.
- #5753 was closed with a comment explaining why the rollback was stale after #5752 succeeded.
- #5412 has no unresolved code review threads and no GitHub review comments, but the required
  large-diff Codex review has not posted.
- #5742 progress remains anchored with `<!-- codex:progress -->` and records the current gate state.
- No direct production mutations were made from the local shell.

## Validation

- PASS: NATS context soak was read before action; live coordination state was refreshed.
- PASS: `gh pr list -R proompteng/lab --state open --search "torghut in:title" --json ...`;
  enumerated open Torghut PRs and selected unblock-first/high-impact release items.
- PASS: `gh pr checks 5747 -R proompteng/lab`; all visible PR checks passed before merge.
- PASS: `gh run view 25460680115 -R proompteng/lab --json status,conclusion,jobs,url,headSha,name`;
  rerun `torghut-ci` passed on `99462489a72abdca059d62cb7a7a31aae597d400`.
- CAPTURED FAILURE: `gh run view 25460680124 -R proompteng/lab --json status,conclusion,jobs,url,headSha,name`;
  `torghut-post-deploy-verify` failed because `torghut` did not become Synced/Healthy at
  `99462489a72abdca059d62cb7a7a31aae597d400` before timeout.
- PASS: `gh pr checks 5752 -R proompteng/lab`; all visible PR checks passed before merge.
- PASS: `gh run view 25461512947 -R proompteng/lab --json status,conclusion,jobs,url,headSha,name`;
  `torghut-ci` passed on `a2c74e20875a3faca77c6651d999a0dfd3465183`.
- PASS: `gh run view 25461512957 -R proompteng/lab --json status,conclusion,jobs,url,headSha,name`;
  `torghut-post-deploy-verify` passed on `a2c74e20875a3faca77c6651d999a0dfd3465183`.
- PASS: `gh run view 25461512958 -R proompteng/lab --json status,conclusion,jobs,url,headSha,name`;
  mainline `argo-lint` passed on `a2c74e20875a3faca77c6651d999a0dfd3465183`.
- PASS: `gh run view 25461512942 -R proompteng/lab --json status,conclusion,jobs,url,headSha,name`;
  mainline `kubeconform` passed on `a2c74e20875a3faca77c6651d999a0dfd3465183`.
- PASS: `kubectl -n argocd get application torghut torghut-options symphony-torghut root -o wide`;
  Torghut child apps are Synced/Healthy at current main revision
  `87e7e222b5dfa4879f27d0e0a0e2975a7237bc5b`.
- PASS:
  `kubectl -n torghut get deployments torghut-00245-deployment torghut-sim-00345-deployment torghut-options-catalog torghut-options-enricher -o wide`;
  active desired-replica Torghut workloads are ready on digest
  `sha256:fcc71697d605875c037aec7a12f35f73dd489f0aaebb6087a51a6e4ec89881be`.
- PASS: `kubectl -n torghut get pods -o wide`; active Torghut runtime pods are Running and ready on
  digest `sha256:fcc71697d605875c037aec7a12f35f73dd489f0aaebb6087a51a6e4ec89881be`.

## Deployment evidence

GitOps state after #5752:

- `torghut`: Synced / Healthy at current main revision
  `87e7e222b5dfa4879f27d0e0a0e2975a7237bc5b`, which includes #5752.
- `torghut-options`: Synced / Healthy at current main revision
  `87e7e222b5dfa4879f27d0e0a0e2975a7237bc5b`, which includes #5752.
- `symphony-torghut`: Synced / Healthy at current main revision
  `87e7e222b5dfa4879f27d0e0a0e2975a7237bc5b`.
- `root`: OutOfSync / Healthy. This remains root ApplicationSet drift, not a Torghut child-app
  rollout failure.

Workload readiness:

- `torghut-00245-deployment`: 1 ready / 1 available / 1 desired on Torghut digest
  `sha256:fcc71697d605875c037aec7a12f35f73dd489f0aaebb6087a51a6e4ec89881be`.
- `torghut-sim-00345-deployment`: 1 ready / 1 available / 1 desired on the same Torghut digest.
- `torghut-options-catalog`: 1 ready / 1 available / 1 desired on the same Torghut digest.
- `torghut-options-enricher`: 1 ready / 1 available / 1 desired on the same Torghut digest.
- Matching active pods are Running and ready. Old Knative revisions are scaled to zero or retained as
  zero-desired historical deployments.

Recent event risks:

- New Torghut, sim, options catalog, and options enricher pods emitted transient startup/readiness
  probe warnings during rollout, then reached Ready.
- Argo recorded healthy revision updates after those warnings.
- ClickHouse still emits `MultiplePodDisruptionBudgets` warnings. This is cluster hygiene debt, not
  a Torghut application readiness blocker for this rollout.

## Risk

- #5412 is the main release risk. It must stay unmerged until Codex review capacity is restored and
  the required large-diff review posts, with all resulting threads resolved, or a maintainer
  explicitly waives the large-diff gate.
- #5747's failed postdeploy showed that an individual promotion can partially apply while the core
  Torghut app is stuck OutOfSync. That is not an active rollback trigger now because #5752
  superseded it and passed postdeploy.
- Root Argo application drift remains a GitOps hygiene risk. Torghut child apps are healthy, so it is
  not a rollback trigger for the current rollout.
- The runner service account cannot rely on direct production mutation for remediation. Rollback and
  promotion remain PR-driven GitOps changes.

## Rollback path

- For #5752: open a GitOps PR reverting `a2c74e20875a3faca77c6651d999a0dfd3465183`, or reverting the
  image-reference changes from #5752 back to the previous healthy digest, then let Argo CD reconcile.
  Do not mutate production directly outside an emergency.
- Previous healthy digest before #5752:
  `sha256:34de81692a3645864cb94deec58950b97c860acce7a3bf5313de47eff95ee08c`.
- If the runtime commit behind image `c2a696c5e9a00215d93edffba5497baa664ca5db` regresses, revert the
  source commit and promote the previous healthy image through the same GitOps PR path.
- Rollback triggers: `torghut` or `torghut-options` becomes Degraded or stuck OutOfSync, the
  post-deploy verification workflow fails on the current head, active Torghut pods lose readiness,
  runtime probes stop returning HTTP 200, or runtime behavior regresses beyond the documented
  live-capital shadow gate.
- #5753 is stale and closed. Do not merge it as a rollback for the current healthy release.
- For #5412: no runtime rollback is required because it was not merged.

## Next action

Keep #5412 open and blocked. The smallest unblocker is restored Codex review capacity, or an explicit
maintainer waiver of the large-diff review gate. After the review posts, resolve any threads, refresh
against current `main`, wait for all required checks, squash-merge, and repeat GitOps/workload/event
verification.
