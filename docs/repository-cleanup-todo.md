# Repository Cleanup TODO

Status: Active, evidence-gated maintenance backlog.

This file tracks cleanup that has a concrete repository target but still needs live ownership, external-consumer, stored
data, or validation evidence before deletion. Each section should land as a focused change. Normal production changes
remain CI/GitOps-managed; the TODO is not authorization for direct cluster mutation.

## Legacy Harvester, Rancher, and K3s assets

- [x] Remove the generic root `tf:*`, `ansible`, `harvester:apply`, `k:install`, and `k:bootstrap` commands so legacy
  infrastructure cannot be invoked accidentally from the repository root.
- [x] Replace root and Kubernetes documentation that advertised those commands with the current Talos/Omni and Argo CD
  source maps.
- [ ] Verify whether any live Harvester environment, remote OpenTofu state, operator workflow, registry consumer, or
  recovery procedure still depends on:
  - `tofu/harvester/**`
  - `tofu/rancher/**`
  - `kubernetes/install.sh`, `kubernetes/install-k3s.ts`, and `kubernetes/devices.json`
  - `ansible/inventory/hosts.ini`, `ansible/inventory/group_vars/k3s_cluster.yml`, and the K3s/Rancher playbooks
- [ ] After that verification, delete only the retired Harvester, Rancher, K3s, and legacy inventory assets and update
  their remaining callers in the same change.
- [ ] Preserve current Talos/Omni material under `devices/galactic/**`, Coder infrastructure, Rook/Ceph canaries and
  benchmarks, and general Kubernetes manifests.

Completion evidence: no live or external consumer, no required remote state, no current documentation reference, clean
repository searches, and focused validation for every retained infrastructure path.

## Unclassified OpenTofu ownership

- [ ] Verify the Cloudflare account owner, remote state, and intended management boundary for `tofu/cloudflare/**`.
- [ ] Add a scoped operator runbook if the stack remains active; otherwise retire it in a focused change.

Completion evidence: one documented owner and state backend for a retained stack, or verified absence of external and
state consumers before deletion.

## Direct-cluster deployment bypasses

- [ ] Remove the generated `kubectl apply` deployment path from `packages/schematic/src/docker/index.ts`, or make the
  generated helper explicitly local/check-only.
- [ ] Remove or convert `packages/scripts/src/app/deploy-service.ts` and the root `app:deploy` alias to check-only behavior.
- [ ] Add fixture coverage proving newly generated services do not contain a direct production apply path.

Completion evidence: generated fixtures and App helpers cannot mutate production desired state, focused tests pass, and
release documentation points only to CI, Kargo, and Argo CD.

## Completed and superseded documentation

- [x] Retire the completed Agents shell Effect/schema modularization plan after confirming the implementation, module
  regression tests, and absence of inbound repository references.
- [x] Retire the pre-Kargo Nix-enabled application rollout evidence report dated 2026-07-05; the current release
  contract now lives in `docs/release-automation.md`.
- [x] Retire the Torghut chip-universe rollout snapshot dated 2026-05-05 after confirming its fixed universe contradicted
  current GitOps configuration and had no inbound references.
- [x] Replace the duplicated 1,189-line `docs/torghut/design-system/v6/index.md` journal with a concise historical
  source map that retains the machine-consumed completion matrix and removes broken catalog entries.

Completion evidence: deleted-document basenames have no remaining inbound references outside this completion record,
every path in the concise v6 source map exists, the Torghut completion-matrix test passes, and repository documentation
validation is clean.

## False-green and duplicated CI

- [ ] Remove `|| true` from the `lint` command in `packages/cx-tools/package.json` and prove lint failures return nonzero.
- [ ] Remove one duplicate `synth:check -- --all` call from each of `.github/workflows/cdk8s-manifests.yml` and
  `.github/workflows/pull-request.yml` while preserving both workflow jobs.
- [ ] Remove the nonexistent `scripts/generate-proto.sh` path filters from both triggers in
  `.github/workflows/buf-ci.yml`.

Completion evidence: focused package lint, `actionlint`, cdk8s synthesis/checks, and Buf workflow path validation pass.

## Agents Argo CD ownership

- [x] Stop current documentation from applying `argocd/applications/agents/application.yaml`; document the Product
  ApplicationSet and `kargo/agents` as the production owner.
- [x] Mark the standalone manifest as legacy and non-authoritative without deleting it in this cleanup.
- [x] Verify the live `Application/agents` is generated by `ApplicationSet/product`, targets `kargo/agents`, and has no
  second owner or nested self-managed Application. Readback on 2026-08-30 confirmed the controller owner reference,
  source path, target revision, and a render with no nested `Application`.
- [ ] After live verification, delete `argocd/applications/agents/application.yaml` and remove its dedicated validation
  reference from `scripts/agents/validate-agents.sh`.

Completion evidence: one live owner, one desired source/revision, no manual bootstrap reference, valid Agents render, and
focused Agents validation.

## Top-level archive

- [ ] Rewrite the retained Harvester-history references in `docs/jangar/self-hosted-model-kubevirt-talos.md` so current
  Talos/KubeVirt guidance does not link into `archive/**`.
- [ ] Confirm nothing under `archive/**` is consumed by active GitOps, code, tests, or maintained runbooks.
- [ ] Delete the top-level archive and use Git history for the retired manifests and documents.

Completion evidence: no live references to `archive/**`, clean documentation links, and no active render or test input
removed.

## Broken image builders

- [ ] Verify no external or manual registry workflow still depends on the images built by:
  - `scripts/build-ecran.sh`
  - `scripts/build-ecran-worker.sh`
  - `scripts/build-ecran-migrator.sh`
  - `scripts/build-juge.sh`
  - `scripts/build-riposte.sh`
- [ ] Confirm `services/ecran`, `services/juge`, and `services/riposte` remain absent and reconcile any related disabled
  GitOps entries before removing the scripts.
- [ ] Delete the five builders and their remaining documentation or workflow references in one focused change.

Completion evidence: absent source trees, no repository or external consumer, no live workload image dependency, and
shell/reference validation for retained builders.

## Bayn compatibility layers

- [ ] Replace the throwing `cancelRequestHash` path in
  `services/bayn/src/broker/alpaca-mutations/decisions.ts` with a typed `Result`, map canonicalization failures into the
  existing execution failure channel, and add malformed broker-order-ID regression coverage.
- [ ] Verify stored-data and historical-read obligations before removing the production-dead daily snapshot-binding path
  spanning `services/bayn/src/cycle/store/**`, `services/bayn/src/db/snapshot-reference.ts`, and its evidence decoders.
- [ ] Split the forward-performance primitives still consumed from `services/bayn/src/market-data/**`, then remove only
  the unused finalized-daily `MarketData` service facade and publication stack.
- [ ] Replace test-only callers of throwing constructors with test-local assertions over the total `Result` APIs, then
  remove the production throwing exports.
- [ ] Remove `services/bayn/src/execution/legacy-authority.ts` and relocate historical behavior constants only after
  proving no persisted protocol decoder or reconciliation path needs them.
- [ ] Preserve append-only migrations, historical decoders, canonical hashes and bytes, forward-performance readers,
  fail-closed readiness, and immutable qualification/evidence contracts throughout the cleanup.

Completion evidence: focused cancellation, coordinator, snapshot, market-data, protocol, and forward-performance tests;
Bayn typecheck and lint; and explicit database-history verification before deleting compatibility readers.
