apiVersion: agents.proompteng.ai/v1alpha1
kind: Agent
metadata:
  name: anypi-eval-agent
spec:
  providerRef:
    name: anypi-eval
  vcsRef:
    name: github
  security:
    allowedSecrets:
      - github-token
      - codex-auth
