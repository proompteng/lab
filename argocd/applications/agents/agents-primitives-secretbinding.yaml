apiVersion: security.proompteng.ai/v1alpha1
kind: SecretBinding
metadata:
  name: agents-primitives-secretbinding
  namespace: agents
spec:
  allowedSecrets:
    - agents-db-app
  subjects:
    - kind: ServiceAccount
      name: codex-workflow
      namespace: argo-workflows
