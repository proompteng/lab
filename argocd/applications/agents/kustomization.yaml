apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: agents
helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: agents
    releaseName: agents
    namespace: agents
    includeCRDs: true
    valuesFile: values.yaml
    version: 0.1.0
