apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jangar-agents-api
  namespace: agents
  annotations:
    agents.proompteng.ai/compatibility-route: "jangar-same-origin-api"
    agents.proompteng.ai/canonical-host: "agents.k8s.proompteng.ai"
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`jangar.k8s.proompteng.ai`) && PathPrefix(`/api/agents`)
      priority: 100
      services:
        - name: agents
          port: 80
    - kind: Rule
      match: Host(`jangar.k8s.proompteng.ai`) && PathPrefix(`/v1/agent-runs`)
      priority: 100
      services:
        - name: agents
          port: 80
  tls:
    secretName: wildcard-k8s-proompteng-ai-tls
