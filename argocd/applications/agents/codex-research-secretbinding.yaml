apiVersion: security.proompteng.ai/v1alpha1
kind: SecretBinding
metadata:
  name: codex-research-secrets
spec:
  subjects:
    - kind: Agent
      name: codex-research-agent
  allowedSecrets:
    - codex-github-token
