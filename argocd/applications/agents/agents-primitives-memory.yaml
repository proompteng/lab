apiVersion: agents.proompteng.ai/v1alpha1
kind: Memory
metadata:
  name: agents-primitives
  namespace: agents
spec:
  type: postgres
  connection:
    secretRef:
      name: agents-db-app
      key: uri
  capabilities:
    - vector
