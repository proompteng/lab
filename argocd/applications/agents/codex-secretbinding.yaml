apiVersion: security.proompteng.ai/v1alpha1
kind: SecretBinding
metadata:
  name: codex-github-token
spec:
  subjects:
    - kind: Agent
      name: codex-agent
  allowedSecrets:
    - github-token
    - codex-auth
