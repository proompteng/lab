apiVersion: security.proompteng.ai/v1alpha1
kind: SecretBinding
metadata:
  name: codex-github-token
spec:
  subjects:
    - kind: Agent
      name: codex-agent
    - kind: Agent
      name: codex-feature-agent
    - kind: Agent
      name: codex-native-workflow
  allowedSecrets:
    - codex-github-token
