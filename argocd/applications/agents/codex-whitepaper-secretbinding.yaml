apiVersion: security.proompteng.ai/v1alpha1
kind: SecretBinding
metadata:
  name: codex-whitepaper-github-token
spec:
  subjects:
    - kind: Agent
      name: codex-whitepaper-agent
  allowedSecrets:
    - github-token
    - codex-auth
