apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: pgadmin
resources:
  - pgadmin-auth-sealedsecret.yaml
helmCharts:
  - name: pgadmin4
    repo: https://helm.runix.net
    version: 1.50.0
    releaseName: pgadmin
    namespace: pgadmin
    valuesFile: values.yaml
patches:
  - path: service-patch.yaml
