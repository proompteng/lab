apiVersion: apps/v1
kind: Deployment
metadata:
  name: torghut-notebooks-hub
spec:
  template:
    spec:
      containers:
        - name: hub
          env:
            - name: CONFIGPROXY_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: torghut-notebook-hub
                  key: proxy-token
