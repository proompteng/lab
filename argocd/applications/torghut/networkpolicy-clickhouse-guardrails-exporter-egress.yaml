apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: torghut-clickhouse-guardrails-exporter-egress
  namespace: torghut
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: torghut-clickhouse-guardrails-exporter
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: torghut
      ports:
        - protocol: TCP
          port: 8123
