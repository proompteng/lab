apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: torghut-simulation-teardown-clean
  namespace: torghut
  labels:
    app.kubernetes.io/name: torghut
spec:
  args:
    - name: runId
    - name: datasetId
    - name: namespace
    - name: taConfigmap
    - name: taDeployment
    - name: torghutService
    - name: signalTable
    - name: priceTable
    - name: postgresDatabase
  metrics:
    - name: teardown-clean
      provider:
        job:
          spec:
            backoffLimit: 0
            ttlSecondsAfterFinished: 300
            template:
              spec:
                serviceAccountName: torghut-runtime
                restartPolicy: Never
                nodeSelector:
                  kubernetes.io/arch: arm64
                containers:
                  - name: analysis
                    image: registry.ide-newton.ts.net/lab/torghut@sha256:bbf054eb10ea04da7cbe4847e5bbe82d9afe2fc95e9f720d7e38327b4b4213c3
                    imagePullPolicy: IfNotPresent
                    env:
                      - name: DB_DSN
                        valueFrom:
                          secretKeyRef:
                            name: torghut-db-app
                            key: uri
                    command:
                      - /bin/bash
                      - -lc
                    args:
                      - >-
                        set -euo pipefail &&
                        cd /app &&
                        /opt/venv/bin/python scripts/run_simulation_analysis.py teardown-clean
                        --run-id "{{args.runId}}"
                        --dataset-id "{{args.datasetId}}"
                        --namespace "{{args.namespace}}"
                        --ta-configmap "{{args.taConfigmap}}"
                        --ta-deployment "{{args.taDeployment}}"
                        --torghut-service "{{args.torghutService}}"
                        --signal-table "{{args.signalTable}}"
                        --price-table "{{args.priceTable}}"
                        --postgres-base-dsn-env DB_DSN
                        --postgres-database "{{args.postgresDatabase}}"
                        --json
