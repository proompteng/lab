apiVersion: v1
kind: Service
metadata:
  name: torghut-metrics
  namespace: torghut
  labels:
    app.kubernetes.io/name: torghut
    app.kubernetes.io/component: metrics
    app.kubernetes.io/part-of: lab
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 8181
      targetPort: 8181
  selector:
    serving.knative.dev/service: torghut
