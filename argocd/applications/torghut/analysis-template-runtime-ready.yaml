apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: torghut-simulation-runtime-ready
  namespace: torghut
  labels:
    app.kubernetes.io/name: torghut
spec:
  args:
    - name: runId
    - name: datasetId
    - name: namespace
    - name: torghutService
    - name: taDeployment
    - name: windowStart
    - name: windowEnd
    - name: signalTable
    - name: priceTable
    - name: runtimeVerifyTimeoutSeconds
    - name: runtimeVerifyPollSeconds
  metrics:
    - name: runtime-ready
      provider:
        job:
          spec:
            backoffLimit: 0
            ttlSecondsAfterFinished: 300
            template:
              spec:
                serviceAccountName: torghut-runtime
                restartPolicy: Never
                nodeSelector:
                  kubernetes.io/arch: arm64
                containers:
                  - name: analysis
                    image: registry.ide-newton.ts.net/lab/torghut@sha256:3560ac50e573cc1ec1b3087e5b763e96b83551e1958b6e3692d9b9006210dcdb
                    imagePullPolicy: IfNotPresent
                    command:
                      - /bin/bash
                      - -lc
                    args:
                      - >-
                        set -euo pipefail &&
                        cd /app &&
                        /opt/venv/bin/python scripts/run_simulation_analysis.py runtime-ready
                        --run-id "{{args.runId}}"
                        --dataset-id "{{args.datasetId}}"
                        --namespace "{{args.namespace}}"
                        --torghut-service "{{args.torghutService}}"
                        --ta-deployment "{{args.taDeployment}}"
                        --window-start "{{args.windowStart}}"
                        --window-end "{{args.windowEnd}}"
                        --signal-table "{{args.signalTable}}"
                        --price-table "{{args.priceTable}}"
                        --runtime-timeout-seconds "{{args.runtimeVerifyTimeoutSeconds}}"
                        --runtime-poll-seconds "{{args.runtimeVerifyPollSeconds}}"
                        --json
