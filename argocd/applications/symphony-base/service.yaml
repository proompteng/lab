apiVersion: v1
kind: Service
metadata:
  name: symphony
  labels:
    app.kubernetes.io/name: symphony
    app.kubernetes.io/part-of: lab
spec:
  selector:
    app.kubernetes.io/name: symphony
  ports:
    - name: http
      port: 8080
      targetPort: http
