apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: torghut-options-catalog
  namespace: torghut
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: torghut-options-catalog
