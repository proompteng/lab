apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-alloy
  namespace: kafka
  labels:
    app.kubernetes.io/name: kafka-alloy
    app.kubernetes.io/component: observability
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: kafka-alloy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kafka-alloy
        app.kubernetes.io/component: observability
    spec:
      serviceAccountName: kafka-alloy
      securityContext:
        fsGroup: 65532
      containers:
        - name: alloy
          image: grafana/alloy:v1.11.2
          imagePullPolicy: IfNotPresent
          workingDir: /var/lib/alloy
          args:
            - run
            - /etc/alloy/config.river
          ports:
            - name: http-metrics
              containerPort: 12345
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            seccompProfile:
              type: RuntimeDefault
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          volumeMounts:
            - name: config
              mountPath: /etc/alloy
            - name: data
              mountPath: /var/lib/alloy
      volumes:
        - name: config
          configMap:
            name: kafka-alloy
            items:
              - key: config.river
                path: config.river
        - name: data
          emptyDir: {}
