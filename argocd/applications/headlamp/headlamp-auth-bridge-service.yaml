apiVersion: v1
kind: Service
metadata:
  name: headlamp-auth-bridge
  namespace: headlamp
  labels:
    app.kubernetes.io/name: headlamp-auth-bridge
    app.kubernetes.io/part-of: headlamp
spec:
  selector:
    app.kubernetes.io/name: headlamp-auth-bridge
  ports:
    - name: http
      port: 8080
      targetPort: http
