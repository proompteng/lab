apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: torghut-cuda-ranker-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 120Gi
