apiVersion: v1
kind: Secret
metadata:
  name: torghut-cuda-ranker-cloud-init-network
type: Opaque
stringData:
  networkData: |-
    version: 1
    config:
      - type: physical
        name: enp1s0
        subnets:
          - type: dhcp
  networkdata: |-
    version: 1
    config:
      - type: physical
        name: enp1s0
        subnets:
          - type: dhcp
