apiVersion: batch/v1
kind: Job
metadata:
  name: torghut-hyperliquid-runtime-db-migrations
  namespace: torghut
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation,HookSucceeded
    argocd.argoproj.io/sync-wave: "-1"
spec:
  backoffLimit: 6
  activeDeadlineSeconds: 900
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        app: torghut-hyperliquid-runtime-db-migrations
    spec:
      restartPolicy: OnFailure
      serviceAccountName: torghut-runtime
      nodeSelector:
        kubernetes.io/arch: arm64
      containers:
        - name: migrate
          image: registry.ide-newton.ts.net/lab/torghut@sha256:b5b68a06cfbc8b14ea8eb55406aecdcf31c971e1cd8ad1fc2585f8fcb1580c7a
          imagePullPolicy: Always
          workingDir: /app
          command:
            - /bin/sh
            - -ec
          args:
            - |
              deadline=$((SECONDS + 300))
              until python - <<'PY'
              import os
              from sqlalchemy import create_engine, text

              dsn = os.environ["DB_DSN"]
              if dsn.startswith("postgresql://"):
                  dsn = "postgresql+psycopg://" + dsn[len("postgresql://"):]
              engine = create_engine(dsn, pool_pre_ping=True)
              with engine.connect() as connection:
                  connection.execute(text("select 1"))
              PY
              do
                if [ "${SECONDS}" -ge "${deadline}" ]; then
                  echo "Postgres did not become ready in time" >&2
                  exit 1
                fi
                sleep 5
              done
              alembic -c /app/alembic.ini upgrade heads
          env:
            - name: TORGHUT_COMMIT
              value: 6a10312c7204319e73bfe7fff9095df0148b336d
            - name: TORGHUT_IMAGE_DIGEST
              value: sha256:b5b68a06cfbc8b14ea8eb55406aecdcf31c971e1cd8ad1fc2585f8fcb1580c7a
            - name: PYTHONPATH
              value: /app
            - name: DB_DSN
              valueFrom:
                secretKeyRef:
                  name: torghut-db-app
                  key: uri
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
