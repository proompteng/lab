apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: speaker
spec:
  template:
    spec:
      containers:
        - name: frr-metrics
          ports:
            - containerPort: 7473
              name: frr-monitoring
