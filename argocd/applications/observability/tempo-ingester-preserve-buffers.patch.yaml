apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: observability-tempo-ingester
spec:
  # These ingesters retain unshipped traces in emptyDir. Preserve their Pod
  # identities until TLS restores uploads and all local blocks are accounted for.
  updateStrategy:
    $patch: replace
    type: OnDelete
