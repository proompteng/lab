apiVersion: v1
kind: Service
metadata:
  name: traefik-rgw
  namespace: traefik
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: traefik-traefik
    app.kubernetes.io/name: traefik
  ports:
    - name: rgws3
      port: 443
      protocol: TCP
      targetPort: rgws3
