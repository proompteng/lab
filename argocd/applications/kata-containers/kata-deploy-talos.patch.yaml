apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: kata-deploy
  namespace: kube-system
spec:
  template:
    spec:
      volumes:
        - name: containerd-conf
          $patch: delete
      containers:
        - name: kube-kata
          volumeMounts:
            - name: containerd-conf
              $patch: delete
