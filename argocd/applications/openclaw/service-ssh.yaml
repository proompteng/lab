apiVersion: v1
kind: Service
metadata:
  name: openclaw-ssh
  labels:
    app.kubernetes.io/name: openclaw
spec:
  type: ClusterIP
  selector:
    kubevirt.io/domain: openclaw
  ports:
    - name: ssh
      port: 22
      protocol: TCP
      targetPort: 22
