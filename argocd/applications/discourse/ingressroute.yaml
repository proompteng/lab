apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: discourse
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - kind: Rule
      match: Host(`forum.<domain>`) # TODO: replace <domain>.
      services:
        - name: discourse
          port: 80
