apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: discourse-shared
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: rook-ceph-block
# TODO: switch to S3-backed uploads if desired (set DISCOURSE_S3_* envs and remove this PVC).
