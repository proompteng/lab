apiVersion: apps/v1
kind: Deployment
metadata:
  name: discourse
  labels:
    app: discourse
    app.kubernetes.io/name: discourse
    app.kubernetes.io/part-of: lab
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: discourse
  template:
    metadata:
      labels:
        app: discourse
        app.kubernetes.io/name: discourse
        app.kubernetes.io/part-of: lab
    spec:
      containers:
        - name: discourse
          image: registry.ide-newton.ts.net/lab/discourse@sha256:1f0b79c1bd8bf817c1ebf9ede5599ea7d3072660723b28c0b59ca13075477823
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: discourse-config
            - secretRef:
                name: discourse-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: discourse-db-app
                  key: uri
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          volumeMounts:
            - name: shared
              mountPath: /shared
      volumes:
        - name: shared
          persistentVolumeClaim:
            claimName: discourse-shared
