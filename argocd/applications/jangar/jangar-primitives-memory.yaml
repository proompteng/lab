apiVersion: agents.proompteng.ai/v1alpha1
kind: Memory
metadata:
  name: jangar-primitives
  namespace: jangar
spec:
  type: postgres
  connection:
    secretRef:
      name: jangar-db-app
      key: uri
  capabilities:
    - vector
