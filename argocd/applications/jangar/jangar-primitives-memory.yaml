apiVersion: memory.proompteng.ai/v1alpha1
kind: Memory
metadata:
  name: jangar-primitives
  namespace: jangar
spec:
  compositionRef:
    name: memory-postgres
  providerRef:
    name: postgres-default
  dataset:
    name: jangar_primitives
    schema: jangar_primitives
  retention:
    ttlDays: 30
  embeddings:
    enabled: true
    dimension: 1536
