apiVersion: security.proompteng.ai/v1alpha1
kind: SecretBinding
metadata:
  name: jangar-primitives-secretbinding
  namespace: jangar
spec:
  allowedSecrets:
    - jangar-db-app
  subjects:
    - kind: ServiceAccount
      name: codex-workflow
      namespace: argo-workflows
