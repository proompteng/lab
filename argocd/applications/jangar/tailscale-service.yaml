apiVersion: v1
kind: Service
metadata:
  name: jangar-tailscale
  namespace: jangar
  annotations:
    tailscale.com/hostname: jangar
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    serving.knative.dev/service: jangar
  ports:
    - name: http
      port: 80
      targetPort: 8012
      protocol: TCP
