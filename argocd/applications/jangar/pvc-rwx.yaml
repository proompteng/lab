apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jangar-workspace-rwx
  namespace: jangar
  labels:
    app.kubernetes.io/name: jangar
    app.kubernetes.io/part-of: lab
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: longhorn
