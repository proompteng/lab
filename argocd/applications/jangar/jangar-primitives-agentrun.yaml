apiVersion: agents.proompteng.ai/v1alpha1
kind: AgentRun
metadata:
  name: jangar-primitives-agentrun
  namespace: jangar
spec:
  agentRef:
    name: jangar-primitives-agent
  implementation:
    inline:
      summary: "Run the jangar primitives smoke implementation"
      text: "Execute the sample implementation for the jangar primitives workflow."
  runtime:
    type: workflow
    config:
      ttlSecondsAfterFinished: 600
  workload:
    image: ghcr.io/proompteng/codex-agent:latest
  parameters:
    repository: proompteng/lab
    base: main
    head: codex/issue-2425-6cedebf4
    stage: implementation
