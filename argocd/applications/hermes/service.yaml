apiVersion: v1
kind: Service
metadata:
  name: hermes
  namespace: hermes
  labels:
    app.kubernetes.io/name: hermes
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hermes
    app.kubernetes.io/component: gateway
  ports:
    - name: api
      port: 8642
      targetPort: api
      protocol: TCP
