apiVersion: v1
kind: Service
metadata:
  name: flamingo
  namespace: flamingo
  labels:
    app.kubernetes.io/name: flamingo
spec:
  selector:
    app.kubernetes.io/name: flamingo
    app.kubernetes.io/component: model-server
  ports:
    - name: http
      port: 80
      targetPort: http
