apiVersion: v1
kind: Service
metadata:
  name: flink-operator-metrics
  namespace: flink
  labels:
    app.kubernetes.io/name: flink-kubernetes-operator
spec:
  selector:
    app.kubernetes.io/name: flink-kubernetes-operator
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
