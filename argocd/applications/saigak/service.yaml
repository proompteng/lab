apiVersion: v1
kind: Service
metadata:
  name: saigak
  labels:
    app.kubernetes.io/name: saigak
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: saigak
    app.kubernetes.io/component: model-server
  ports:
    - name: http
      port: 11434
      protocol: TCP
      targetPort: 11434
    - name: embed
      port: 11435
      protocol: TCP
      targetPort: 11434
