apiVersion: v1
kind: Service
metadata:
  name: symphony
  namespace: jangar
  labels:
    app.kubernetes.io/name: symphony
    app.kubernetes.io/part-of: lab
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: symphony
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
