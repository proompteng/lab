apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: transactor
spec:
  entryPoints:
    - websecure
  tls:
    secretName: huly-wildcard-tls
  routes:
    - match: Host(`transactor.huly.proompteng.ai`)
      kind: Rule
      services:
        - name: transactor
          port: 80
