apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: front
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`huly.proompteng.ai`)
      kind: Rule
      services:
        - name: front
          port: 80
