apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: bayn-db-proof-20260819t131313z
  labels:
    app.kubernetes.io/name: bayn-db-proof-20260819t131313z
    app.kubernetes.io/part-of: bayn
    app.kubernetes.io/component: recovery-drill
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  podSelector:
    matchLabels:
      cnpg.io/cluster: bayn-db-proof-20260819t131313z
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              cnpg.io/cluster: bayn-db-proof-20260819t131313z
      ports:
        - port: 5432
          protocol: TCP
        - port: 8000
          protocol: TCP
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: cloudnative-pg
          podSelector:
            matchLabels:
              app.kubernetes.io/name: cloudnative-pg
      ports:
        - port: 5432
          protocol: TCP
        - port: 8000
          protocol: TCP
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
          podSelector:
            matchLabels:
              app.kubernetes.io/name: observability-cluster-metrics-alloy
      ports:
        - port: 9187
          protocol: TCP
