disableLookups: true
neo4j:
  name: graf
  password: ""
  passwordFromSecret: graf-auth
volumes:
  data:
    mode: dynamic
    dynamic:
      storageClassName: longhorn
      accessModes:
        - ReadWriteOnce
      requests:
        storage: 20Gi
