apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: rook-ceph

# This source is intentionally separate from the CephCluster Application.
# Root syncs it only after the read-only storage preflight proves the upgraded
# data plane and CSI rollout. The source contains functional evidence only;
# CephX HEALTH_WARN remains a separate strict acceptance condition.
resources:
  - storage-canary.yaml
